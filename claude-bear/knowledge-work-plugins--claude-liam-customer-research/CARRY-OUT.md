# CARRY-OUT.md (GATE C)

**Carry-out line (written first):**

> A skill isn't a new power Claude has — it's a written set of steps.
> customer-research turns your question into the same multi-source,
> attributed findings, every single time.

**Test:** if someone repeats only this in a meeting next week, is it still
true? Yes — it compresses the distinction that matters (skill = instructions,
not a hidden capability) plus the one concrete, checkable behavior (the same
research-and-attribute treatment from the same steps).

**Wrong guess it defeats:** that a "skill" gives Claude some new ability it
didn't have before — specifically, that `customer-research` means Claude
already knows or remembers things about your customers. It doesn't; it
looks up the sources you give it and attributes what it finds, and the
skill is only the instructions for how to do that lookup.

**Signed:** GATE C — SIGNED, 2026-09-03 (unattended build; no human reviewer
in the loop for this invocation, per COMPLETION LAW / loop contract).
