# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> When you've changed one file, push.py sends just that file — install_apps.py
> resyncs everything, whether it changed or not. Match the script to the size
> of the change, not habit.

**Wrong guess it defeats:** that a one-file change means reinstalling the
whole app directory (B00's hesitant-writer correction: "reinstall" → "push"
— the newcomer's naive assumption is that any change means the full-sync
tool; the corrected question asks whether a single-file push script exists,
which is what the rest of the reel shows: install_apps.py and push.py both
can push one file, but only push.py is built for that job, and nothing in
either script's name signals which one is faster for the change in front
of you).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole dev
loop.
