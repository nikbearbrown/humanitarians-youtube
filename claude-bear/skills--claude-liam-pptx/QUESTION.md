# QUESTION — skills--claude-liam-pptx (redo)

Redo-mode source question (from `PEDAGOGY.md`'s PREDICT, source reel
`claude-liam-pptx`): "Can't Claude just write python-pptx to make slides?
Why does this need its own skill?"

Reframed for the Plain-register hesitant-writer open: "Do you need to
learn python-pptx to build slides?" — corrected on screen to "Do you need
to skip python-pptx to build slides?" (the skill does not reach for
python-pptx at all; it routes across three other tools depending on the
task).

Asked by: nobody named — a general newcomer's first assumption about how
Claude builds a slide deck. Name not applicable (not a real pasted
question; the source was a skill-teardown reel, not a user-submitted ask).
