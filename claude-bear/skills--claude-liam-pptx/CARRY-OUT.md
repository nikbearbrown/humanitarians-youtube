# CARRY-OUT — skills--claude-liam-pptx

**The line (BCRY, written before any other narration):**

> Making slides with Claude means picking the right tool for the job, not
> writing one script. And no render counts as done until a subagent has
> actually looked at it.

**The wrong guess it defeats:** that building a slide deck is one script
(reach for python-pptx and write it) — the PREDICT question from the
source's PEDAGOGY.md. The reel routes that guess into two separate wrong
guesses instead, per Plain register: B00's hesitant-writer correction
("learn" → "skip" python-pptx) carries the top-level guess; the body's
dedicated WRONG-GUESS/BREAK beat (NB03/NB04) carries a second, more
specific one a viewer who already knows the skill exists would still make
— that the one tool that creates a deck from scratch (pptxgenjs) must also
be the tool that edits an existing one.

**Test:** if someone repeats only this line next week — "route to the
right tool, and QA it before calling it done" — it is still true, and it
compresses the distinction that matters (skill-with-mandate vs. ad hoc
script), not the topic (pptx).
