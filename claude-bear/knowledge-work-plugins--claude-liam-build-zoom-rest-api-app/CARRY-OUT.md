# CARRY-OUT — Claude, Build Zoom Rest Api App.

**Carry-out sentence:**
Claude repeats the exact same Zoom REST API guidance every time — but only
for the five things this skill actually writes down.

**Wrong guess it defeats:**
That naming the skill hands Claude general, out-of-the-box Zoom API
expertise. It doesn't — it reads one file, and the file only covers five
named things (endpoint selection, resource-management patterns, OAuth
requirements, rate-limit awareness, API error debugging). Same input to
that file, same output, every run; ask something the file never lists and
there's nothing written to read.

Test: repeatable in a meeting next week — "the skill only repeats what's in
the file, nothing more" — still true, still the distinction that matters,
not a restatement of the topic.
