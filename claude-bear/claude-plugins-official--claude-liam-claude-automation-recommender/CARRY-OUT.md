# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> It analyzes your codebase and tells you what to add and why — it never
> creates a file, and it doesn't always hand you the exact command to run
> either. Recommending isn't installing.

**Wrong guess it defeats:** that asking Claude for automation
recommendations means Claude sets them up for you (B00's hesitant-writer
correction: "build" → "recommend" — the naive framing asks if Claude will
build the automations; the corrected question asks whether it will
recommend them, which is what the rest of the reel shows: the skill is
read-only end to end, and even its recommendations sometimes stop short of
a paste-ready command — a subagent gets a template pointer instead of a
scaffold, a plugin gets a name instead of an install command).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
