# QUESTION.md

**Question:** My plugin (or the skill inside it) isn't showing up, even though
I configured it — is my configuration broken, or is something else going on?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-debug-plugins`), a fully-built Teardown
reel under
`anthropics/claude-tag-plugins/youtube/claude-liam-debug-plugins/`
that walks the Claude Code `debug-plugins` Skill (a diagnostic ladder for
plugin/skill loading run from inside the session container). Not a live
viewer submission.

**Name usable:** N/A (no submitter).
