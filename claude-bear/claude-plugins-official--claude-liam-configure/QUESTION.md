# QUESTION.md

**Question (as posed by SUBJECT.json / redo source):** "Configure" — once you
save a new Discord bot token through the `configure` skill, is the bot
immediately running it?

**Who asked:** synthetic — this is a `hai-simple` redo of an existing
Teardown-register reel (`claude-liam-configure`), not a user-submitted
question. The source reel's own dispatch triggers ("paste a Discord bot
token, ask to configure Discord, ask how to set this up") are the closest
thing to an asker; the specific question above is the one the redo's
BrutalistHesitantWriter cold open poses and the carry-out resolves.

**May the name be used:** N/A — no named asker.
