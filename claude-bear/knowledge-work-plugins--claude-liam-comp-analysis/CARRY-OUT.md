# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> A Skill doesn't give Claude judgment about pay — it makes Claude run your
> benchmarking steps, in order, every time.

**Test:** repeated cold, out of context, next week — still true, and still specific to
what a Claude Skill actually is (a fixed instruction file Claude reads and executes in
order), not a generic "AI can crunch comp numbers" line. It compresses the distinction
that matters (a Skill is a followed procedure, not independent judgment about fair pay),
not the topic (compensation benchmarking in general).

**Wrong guess it defeats:** that `comp-analysis` is an app that decides what to pay
someone on its own. B00 states that framing and corrects it on screen ("app" → "skill");
B01 shows the skill is just a folder holding one instruction file; B03 states the
skill's actual, specific job (benchmark pay, place roles in bands, model equity grants)
and its scope limit (only what the page says, same result every run). The carry-out is
what a viewer should keep: the reliability comes from Claude following fixed steps, not
from Claude exercising its own judgment about compensation.

**Source of this line:** compressed from the source reel's `BVDT` verdict-recap
narration ("comp-analysis makes Claude execute one task reliably... Same input, same
output, every run. Know the limit: only what the file says.") — reused as this video's
single carry-out, generalized ("A Skill doesn't give Claude judgment...") so it reads as
a fact about Claude Skills generally, illustrated by comp-analysis, rather than a
verdict on this one skill's design. A verdict belongs to Teardown, not Plain; this one
sentence does the job without that framing — same generalization pattern used across
this family's sibling skill-teardown redos
(`financial-services--claude-liam-investment-proposal`, `...-ic-memo`, `...-buyer-list`).
