# QUESTION.md

**Question:** Isn't creating a Claude skill just writing one good prompt — why does
the Skill Creator need its own workflow?

**Asked by:** framed by this redo, not a real inbound viewer question — `skill-creator`
is an Anthropic skill (a `SKILL.md` Claude reads before it works), and the natural
newcomer question when shown "make Claude build a skill" is "isn't that just prompt
engineering?"

**Name usable:** N/A (no real asker; redo of `claude-liam-skill-creator`, a
Teardown-register skill explainer built 2026-07-18 from
`anthropics/skills/skills/skill-creator/SKILL.md` — the source reel's own
`beats[*].narration_text` carries the facts instead).

**Mode:** redo. Source: `anthropics/skills/youtube/claude-liam-skill-creator/beat_sheet.json`
(7 beats: B00, B01, B02, B05, BVDT, BHTF, BOUT — Teardown register, no SCRIPT.md — its
`beats[*].narration_text` is the locked narration per the redo contract).
