# QUESTION.md

**Question:** When you wire an external service into a Claude Code plugin
over MCP, is there one standard way to hook it up — or does the right setup
depend on where and how that service actually runs?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-mcp-integration`), a fully-built Teardown
reel under
`anthropics/claude-plugins-official/youtube/claude-liam-mcp-integration/`
that walks the Anthropic `mcp-integration` Claude Code plugin-dev Skill. Not
a live viewer submission.

**Name usable:** N/A (no submitter).
