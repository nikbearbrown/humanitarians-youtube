# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Match the transport to where the service actually lives — stdio for
> local, SSE for hosted OAuth, HTTP for tokens, WebSocket for real-time.
> Then pre-allow the exact tool name, because one character off and it
> fails silently, not loudly.

**Wrong guess it defeats:** that the important decision is which API key to
use (B00's hesitant-writer correction: "key" → "transport" — the naive
framing asks what key the plugin needs; the corrected question asks what
transport it needs, which is what the rest of the reel shows: four
transport types matched to where a service runs, a naming convention for
pre-allowed tools that must match character for character, and the concrete
cost of getting that match wrong — no error, the tool is just not there).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
