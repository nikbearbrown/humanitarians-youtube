# QUESTION — claude-quickstarts--browser-coordinate-scaling

**Question (from the source sheet, carried unchanged):** Bridging the Pixel Gap in
Browser Automation — when Claude's browser tool reports a click coordinate, why
doesn't that number land on the button when you click it on your actual screen?

**Asked by:** no named human asker — this is a `mode: "redo"` build. The source is
`anthropics/claude-quickstarts/youtube/browser-coordinate-scaling/beat_sheet.json`,
an unfinished Teardown-register scaffold (4/8 beats filled, no SCRIPT.md) built
from `browser-use-demo/browser_use_demo/tools/coordinate_scaling.py`.

**Name usable:** n/a (no human asker).

**Note on duplication:** the identical question, sourced from the identical
underlying fact set, was already built and delivered as
`hai-simple/claude-basics--browser-coordinate-scaling` on 2026-08-28 (from a
*different* source-sheet path, `anthropics/youtube/claude-basics/browser-coordinate-scaling/`).
`queue_scan.py --from-sheets` queues every `beat_sheet.json` under `anthropics/`
independently, by design (no cross-path dedup), so this is a second, separately
sourced redo target rather than a re-run of the same job. Logged for transparency;
proceeding per the supervisor's per-reel assignment. This build uses fresh
narration and a new worked anchor example so the two reels are not literal
duplicates of each other's output.
