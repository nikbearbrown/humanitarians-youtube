# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> Same corpus in, same cited answers out, every time — deciding whether to
> sign is still yours.

**Test:** repeated cold, out of context, next week — still true, still
specific to this skill (names the actual behavior: a repeatable,
citation-backed question-answering pass over a set of contract files), not a
generic "AI can read contracts" line. It compresses the distinction that
matters (repeatable, cited retrieval vs. who decides to sign), not the topic
(contract review in general).

**Wrong guess it defeats:** that "Claude, Contracts" means Claude reads your
contract and tells you whether it's safe to sign — a legal judgment. The
reel's B03 states the skill's actual constraint (answer a question across a
corpus of contract documents, cite exactly where each answer comes from) and
stops there without ruling on whether that's a good or bad design; the
carry-out is what a viewer should keep: the skill's job is repeatable,
cited retrieval, scoped to the corpus it's given — the actual decision to
sign, or not, is still the reader's.

**Source of this line:** compressed from the source reel's `BVDT`
verdict-recap narration ("contracts makes Claude execute one task reliably.
The SKILL.md is the spec... Same input, same output, every run. Know the
limit: only what the file says.") — reused as this video's single global
carry-out. The redo's only change is which beat carries it (a REMOTION quote
card, one sentence, alone on screen) instead of a verdict-artifact card with
a "Verdict" label; a verdict belongs to Teardown, not Plain, and this one
sentence already does the job without the framing.
