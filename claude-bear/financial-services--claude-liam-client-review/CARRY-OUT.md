# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Claude skill isn't a hidden analysis app — it's a plain-text file of
> instructions, so the same account data builds the same meeting-ready
> packet every time. Whatever that file doesn't say, Claude has to work out
> on its own.

**Wrong guess it defeats:** that a domain-specific capability named
"client-review" means Claude is analyzing the portfolio and deciding what
matters for the meeting (B00's hesitant-writer correction: "app" → "skill"
— the naive framing asks whether the client-review app analyzes the
portfolio; the corrected question asks whether the skill does, which is
what the rest of the reel answers: a skill is a folder with one file,
SKILL.md, that Claude reads and executes step by step, and the file's
coverage — the performance summary, allocation analysis, talking points,
and action items it names — is the only thing that repeats; anything it
doesn't specify, Claude reasons past on its own).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
