# CARRY-OUT.md

**Carry-out line (BCRY, read before any other narration — CARRY-OUT LAW):**

> A Skill doesn't make Claude smarter — it makes Claude follow your steps, in order,
> every time.

**Test:** repeated cold, out of context, next week — still true, and still specific to
what a Claude Skill actually is (a fixed instruction file Claude reads and executes in
order), not a generic "AI can write memos" line. It compresses the distinction that
matters (a Skill is a followed procedure, not independent judgment), not the topic (IC
memo drafting in general).

**Wrong guess it defeats:** that `ic-memo` is an app that writes the investment committee
memo on its own. B00 states that framing and corrects it on screen ("app" → "skill"); B01
shows the skill is just a folder holding one instruction file; B03 states the skill's
actual, specific job (draft a structured IC memo from due diligence findings, financial
analysis, and deal terms, synthesized into one document) and its scope limit (only what
the page says, same result every run). The carry-out is what a viewer should keep: the
reliability comes from Claude following fixed steps, not from independent judgment about
the deal.

**Source of this line:** compressed from the source reel's `BVDT` verdict-recap narration
("ic-memo makes Claude execute one task reliably... Same input, same output, every run.
Know the limit: only what the file says.") — reused as this video's single global
carry-out, generalized slightly ("A Skill doesn't make Claude smarter...") so it reads as
a fact about Claude Skills generally, illustrated by ic-memo, rather than a verdict on
this one skill's design. A verdict belongs to Teardown, not Plain; this one sentence does
the job without that framing.
