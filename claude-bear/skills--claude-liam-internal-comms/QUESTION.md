# QUESTION.md

**Question:** Doesn't Claude already know how to write a status update? When
you ask Claude to write a team update, does it just write one — however it
sounds best — or is something else going on first?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-internal-comms`), a fully-built Teardown
reel under `anthropics/skills/youtube/claude-liam-internal-comms/` that walks
the Anthropic `internal-comms` Claude Skill (`PEDAGOGY.md`'s stated PREDICT
question). Not a live viewer submission.

**Name usable:** N/A (no submitter).
