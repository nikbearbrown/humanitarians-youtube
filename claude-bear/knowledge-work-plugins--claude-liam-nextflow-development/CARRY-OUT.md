# CARRY-OUT.md (GATE C)

**Carry-out line (adapted from source's verdict beat, register already mostly
factual — judgment framing removed, wording tightened):**

> nextflow-development isn't trained knowledge — it's a file Claude reads
> before it acts. Run rnaseq, sarek, or atacseq, and it runs the same steps
> every time. Know the edge: only what the file says.

**Wrong guess it defeats:** that a Claude skill named after a specialist
domain (bioinformatics pipelines) means Claude was trained on that domain,
the way a person studies a field — rather than being handed one instruction
file for one fixed job, read fresh each time it's used.

**Secondhand test:** *"The skill doesn't know biology, it reads a file that
tells it what to do — same input, same output, and nothing outside the
file."* Still true if repeated by someone who wasn't paying full attention.
It compresses the distinction that matters (instructions vs. training), not
the topic (this specific pipeline family).

**Signed:** SIGNED — carried over from source verdict beat (BVDT), register
changed Teardown → Plain; no new judgment introduced.
