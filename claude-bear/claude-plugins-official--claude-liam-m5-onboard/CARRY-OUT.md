# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> One script can detect, identify, flash, and install this board — every
> stage but one. The button press stays a human's job. No flag replaces a
> thumb.

**Wrong guess it defeats:** that automating a workflow with "one command"
means the whole thing runs with zero hands-on steps (B00's hesitant-writer
correction: "No human steps" → "One button dance" — the naive framing asks
whether the whole flash is hands-off; the corrected question names the one
step that still isn't: on native-USB boards, entering flashing mode has no
software path, so the script waits for a person to physically hold one
button, tap reset while still holding it, hold a beat longer, then let go).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole
workflow.
