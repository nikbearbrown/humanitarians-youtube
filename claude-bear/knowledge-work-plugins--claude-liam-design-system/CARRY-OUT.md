# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> design-system doesn't invent a new look — it checks your components
> against the patterns already in your codebase, and matches or extends what
> is already there.

**Wrong guess it defeats:** that Claude keeps a design system consistent
using its own taste (B00's hesitant-writer correction: "taste" →
"its SKILL.md" — the naive framing asks if taste keeps the system
consistent, the corrected question asks whether the SKILL.md does, which is
what the rest of the reel shows: the skill is a folder Claude reads before
it works, the pipeline reads and executes its Steps section in order, and
the whole thing covers exactly one job — auditing, documenting, or
extending a design system against what's already there, never inventing a
fresh aesthetic).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
