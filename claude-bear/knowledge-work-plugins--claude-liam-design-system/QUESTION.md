# QUESTION.md

**Question:** When Claude audits, documents, or extends a design system, is
it using its own good taste about what looks right — or something else
entirely, like a fixed instruction file it reads first?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-design-system`), a fully-built Teardown
reel under
`anthropics/knowledge-work-plugins/youtube/claude-liam-design-system/` that
walks the Anthropic `design-system` Claude skill (audit, document, or extend
a design system). Not a live viewer submission.

**Name usable:** N/A (no submitter).
