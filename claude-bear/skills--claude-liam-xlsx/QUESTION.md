# QUESTION — skills--claude-liam-xlsx (hai-simple redo)

Redo of `anthropics/skills/youtube/claude-liam-xlsx` (Teardown, Claude
audience). No new asker — the question is inherited from the source's
learning objective (PEDAGOGY.md): "never hardcode — always write Excel
formulas" is called out as the skill's single absolute rule, so that rule
is this reel's spine.

Newcomer framing (what B00's hesitant writer types first): "Does Claude
just type the number in, once a total's computed?" — corrected on screen
to "...type the formula in..."

Real question this reel answers: When Claude builds you a spreadsheet with
a computed total, does it matter whether that total lands in the cell as a
plain number or as a live formula?
