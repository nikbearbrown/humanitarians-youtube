# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude writes the design philosophy first, then draws from it — and the
> make-it-better pass that follows is already written into the skill, so it
> runs whether the first draft needed it or not.

**Wrong guess it defeats:** that asking Claude for a poster returns a picture
immediately (B00's hesitant-writer correction: "immediately" → "eventually"
— the naive framing assumes the request goes straight to a drawn result; the
corrected question concedes a picture does come back, but only after a step
that happens first: a written design philosophy that names an aesthetic
movement and five visual dimensions before a single pixel is generated).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
