# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude doesn't decide a ticket deserves a knowledge base article because
> it feels that way — it checks the same written triggers every time. What
> it covers is exactly what that file names, and nothing past it.

**Wrong guess it defeats:** that Claude judges, on its own writing sense,
when a resolved issue is "worth" turning into an article (B00's
hesitant-writer correction: "instinct" → "a skill file" — the naive framing
asks if Claude drafts a knowledge base article using instinct; the
corrected question asks whether it does so using a skill file, which is
what the rest of the reel shows: a folder Claude reads before it acts,
whose file names a specific list of triggers — a resolved issue worth
documenting for self-service, a recurring question, a workaround to
publish, or a known issue to communicate — and nothing outside that list).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of what kb-article
does.
