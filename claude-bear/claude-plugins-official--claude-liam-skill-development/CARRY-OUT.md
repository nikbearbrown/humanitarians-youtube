# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Skill isn't code Claude runs — it's plain instructions Claude reads and
> follows itself. So it only ever does exactly what the words on the page
> say, nothing more and nothing hidden.

**Wrong guess it defeats:** that building a Skill means writing code Claude
executes (B00's hesitant-writer correction: "code" → "write" — the naive
framing asks how to *code* a new Skill; the corrected question asks how to
*write* one, which is what the rest of the reel shows: SKILL.md is a plain-
language instruction set with no hidden logic, Claude reads it and acts, and
because nothing runs underneath the words, the skill's behavior is exactly
as reliable — and exactly as limited — as what the file actually says).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
