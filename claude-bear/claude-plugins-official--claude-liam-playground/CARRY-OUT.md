# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> The playground's real output is a prompt, not a list of settings. If what
> it hands back can't be copied straight into Claude and acted on, it's not
> finished — it's a value dump.

**Wrong guess it defeats:** that a Claude-built "playground" is just a
settings panel that hands you back the values you picked (B00's
hesitant-writer correction: "values" → "a prompt" — the naive framing asks
whether Claude will build a panel that hands back values; the corrected
question asks whether it hands back a prompt, which is what the rest of the
reel shows: the state invariant that keeps the preview honest, the
natural-language rule the prompt output has to follow, and the fact that
nothing in the skill enforces that rule automatically — so checking is the
required last step, not an afterthought).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
