# CARRY-OUT — skills--claude-liam-claude-api

**Sentence (BCRY, written first per CARRY-OUT LAW):**

> Claude doesn't recall the current Claude API from memory — it checks a
> reference before writing. It only catches the drift someone bothered to
> document.

**Test:** if someone repeats only this in a meeting next week, is it still
true? Yes — it compresses the distinction (check-first, not omniscient
recall) without overclaiming reliability (the second sentence is the
BOTH-DIRECTIONS hedge, carried into the carry-out itself since NB11 is this
reel's one flag).

**Wrong guess it defeats:** that Claude, as Anthropic's own model, simply
already knows the current shape of Anthropic's own API. NB03/NB04 break
this with a concrete case — the `budget_tokens` parameter, which now
returns a 400 error instead of a warning, meaning training memory alone
would ship broken code.
