# QUESTION — skills--claude-liam-claude-api (hai-simple redo)

Redo of `anthropics/skills/youtube/claude-liam-claude-api` (Teardown, Claude
audience). No new asker — the question is inherited from the source's
learning objective (PEDAGOGY.md): does Claude already know the current
Claude API, or does something make it check first?

Newcomer framing (what B00's hesitant writer types first): "Doesn't Claude
already know its own API?" — corrected on screen to "check."

Real question this reel answers: When Claude writes code that calls the
Claude API, what makes it check a reference instead of writing from a
(possibly stale) training memory?
