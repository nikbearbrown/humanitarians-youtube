# QUESTION — claude-quickstarts--stable-element-refs

**Question (from the source sheet, carried unchanged):** Stable Reference IDs Survive
Viewport Chaos — when a browser gets resized and the page reflows, why does an
automation that clicked a button by pixel coordinate stop working, and what actually
keeps working across the resize?

**Asked by:** no named human asker — this is a `mode: "redo"` build. The source is
`anthropics/claude-quickstarts/youtube/stable-element-refs/beat_sheet.json`, a
Teardown-register scaffold (4/8 beats filled with Manim, no SCRIPT.md), sourced from
`browser-use-demo/browser_tool_utils/` per the source's own SOURCES.md (an
illustrative worked example, not a locally-inspectable code file in this checkout —
see SOURCES.md here for how that's handled).

**Name usable:** n/a (no human asker).
