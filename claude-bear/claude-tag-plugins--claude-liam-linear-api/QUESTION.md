# QUESTION.md

**Question:** You're pointing Claude at Linear's API for the first time — where
do you actually send a call, and how do you tell whether it worked?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-linear-api`), a fully-built Teardown reel
under `anthropics/claude-tag-plugins/youtube/claude-liam-linear-api/` that
walks the `linear-api` Skill (GraphQL model, two ID systems, auth, error
checking, rate limits). Not a live viewer submission.

**Name usable:** N/A (no submitter).
