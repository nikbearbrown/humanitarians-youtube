# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude doesn't improvise the close — it runs the same written steps every
> time, on any numbers you give it. The edge of what it does is exactly the
> edge of what that file says.

**Wrong guess it defeats:** that Claude closes the books using its own
accounting judgment (B00's hesitant-writer correction: "judgment" → "a skill
file" — the naive framing asks if Claude can close the books using judgment;
the corrected question asks whether it does so using a skill file, which is
what the rest of the reel shows: a fixed four-step pipeline — reconcile,
flag, write, export — run the same way on every input, with no step outside
what the file specifies).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of what close-month
does.
