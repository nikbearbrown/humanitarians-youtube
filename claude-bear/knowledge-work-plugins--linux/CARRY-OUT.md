# CARRY-OUT — knowledge-work-plugins--linux

**Wrong guess it defeats:** a Zoom meeting bot sounds like it needs a screen or a window
somewhere — some visible place it sits inside the call. It doesn't; the source's own spec
says "headless meeting bots ... for server-side automation" — no window, no one watching.

**Carry-out line (written first, before any other narration):**

> meeting-sdk/linux isn't a Zoom app with a window — it's one instruction file that runs
> Claude as a headless bot on a server, doing exactly what that file says, every time.

**Survives-repetition test:** someone repeating only this sentence next week is still
describing what the `meeting-sdk/linux` skill does correctly — it compresses the
distinction that matters (no window, one file, the same behavior every run) rather than
summarizing the topic.
