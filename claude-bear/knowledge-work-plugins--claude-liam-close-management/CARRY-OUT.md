# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Follow the close-management spec, and Claude runs the same close the same
> way, every time — but only for what the spec actually names.

**Wrong guess it defeats:** that close-management *automates* the month-end
close — runs it for you, end to end, on its own judgment (B00's
hesitant-writer correction: "automate" → "structure" — the naive framing asks
if the skill automates the whole close by itself; the corrected question asks
whether it structures the way Claude runs it, which is what the rest of the
reel shows: a SKILL.md is a folder-and-file Claude reads before acting, the
Steps section is a linear, non-branching sequence Claude executes in order,
and the whole thing is a bounded specification — repeatable for exactly what
it names, silent on everything it doesn't).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
