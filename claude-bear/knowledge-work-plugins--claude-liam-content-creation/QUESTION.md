# QUESTION.md

**Question (as redone for hai-simple):** When Claude "does content creation,"
is that one piece of writing you paste everywhere, or does it actually change
per channel?

**Source:** redo of
`anthropics/knowledge-work-plugins/youtube/claude-liam-content-creation`
(a rendered Teardown-register `claude-liam` reel walking through the
`content-creation` Anthropic skill — a marketing-content drafting tool).

**Asker:** nobody named — the source reel framed this as a general skill
teardown, not a specific person's question. Name not applicable.

**Locked facts carried over (do not alter):** the skill runs from a SKILL.md
instruction file (a "skill" = a folder Claude reads before acting);
execution is linear — read the file, work each step in order, return the
result; the skill drafts marketing content across exactly six channel
types — blog posts, social media, email newsletters, landing pages, press
releases, and case studies; the trigger for using it is writing any
marketing content, needing channel-specific formatting, SEO-optimized copy,
headline options, or calls to action; the boundary is that only what the
instruction file specifies gets covered — same input, same output, every
run.
