# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude checks a competition proof only after hiding the reasoning that
> built it — from a verifier that can't agree with logic it's never seen,
> and would rather say no confident solution than guess.

**Wrong guess it defeats:** that showing the checker everything — the full
reasoning trail, not just the answer — would help it decide (B00's
hesitant-writer correction: "everything" → "just the proof" — the naive
framing asks if showing the checker *everything* works; the corrected
question asks if showing it *just the proof* works, which is what the
rest of the reel shows: the interpretation
check and parallel solving that produce the proof, the thinking trace
stripped before any verifier sees it, the pattern-armed adversarial check
and its asymmetric vote, and the concrete reason a verifier that sees the
reasoning can't be trusted the same way).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
