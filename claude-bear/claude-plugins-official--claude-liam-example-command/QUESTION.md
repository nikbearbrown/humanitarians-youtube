# QUESTION.md

**Question:** What's actually inside a Claude slash command file — and does matching the reference format mean the command works?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-example-command`, a Teardown review of the
`example-command` reference skill under `claude-plugins-official`). Not a live
viewer submission.

**Name usable:** N/A (no submitter).
