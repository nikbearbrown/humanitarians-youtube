# Claude, Month End Prep — Narration Script (GATE P)

*Skill: `hai-simple` (redo of a `skill-teardown` sheet). Register: **Plain**.
7 beats ≈ 1:45–2:00, matching the source's beat count.*
*Carry-out written first (CARRY-OUT.md, GATE C). Every beat lands it.*

**Narrator:** Liam, Kokoro `am_onyx`. No puppet host — B00 is the Brutalist
Hesitant Writer (hai-simple WRITER LAW).

| Beat | Move | Narration | Visual |
|---|---|---|---|
| **B00** | hesitant writer cold open | "How does Claude learn a skill like month-end-prep? It doesn't learn it — there's no training step. It reads a file, the same one, every time it runs. So what's actually written in that file?" | Writer types "How does Claude learn a skill like month-end-prep?", hesitates on "learn", corrects to "read" |
| B01 | 1 stakes / 4 ANCHOR PLANTED | A Skill is a folder Claude reads before it acts, and month-end-prep is a real one: a SKILL.md file holding the whole instruction set in plain language, plus a small reference folder beside it. Nothing hidden, nothing trained — the file is the entire program, and it's the same file every single time Claude opens it. | THE ANCHOR — the month-end-prep folder: SKILL.md (accented) + reference/ |
| B02 | 3 mechanism | Inside, the steps run top to bottom. Claude reads the first step, does exactly that, then moves to the next — no branching, no skipping, unless a step itself says to. Run the file in January or run it in June, and it opens to the identical steps both times. | Steps executing in order; JAN and JUN both pointing at the same unchanged file |
| B03 | 4 ANCHOR PAYOFF / 5 both directions | That's the guarantee: the same file, run twice, produces the same checklist — reliable, repeatable, nothing improvised. But it cuts both ways. Follow a step exactly and the close goes the same way every month. Leave something the month actually needs out of the file, and Claude doesn't invent it — the skill only ever does what the file says, never what the file doesn't. | THE ANCHOR RETURNS — the same folder, run in JAN and JUN, identical checklists both times; an "improvises" bubble struck through |
| **BCRY** | **6 carry-out** | A Skill is a file Claude reads before it acts, not a memory it draws on — the same steps, every run, and never more than what the file says. | the sentence, alone, serif, large |
| BHTF | handoff | Your turn. Here's the prompt — read it with me: "Read the month-end-prep skill. Before you do anything, walk me through each step you're about to take, and point to the line in the file it comes from." Watch whether every step traces back to an actual line — if one doesn't, that's Claude going past what the file says. Liam, in for Bear. | ClaudeComposerAsk, "Your turn." |
| BOUT | outro | Claude, Month End Prep. Liam, in for Bear. | OutroCTA, @HumanitariansAI |

## Register audit (Plain)

| Check | Where |
|---|---|
| Stakes before mechanism | B00 states the naive framing; the file-reading mechanism is not detailed until B01–B02 |
| Wrong guess surfaced *and falsified by a case* | B00 states the "Claude learns it" read; the correction ("no training step... the same file, every time") falsifies it immediately, and B01/B02 show the file being re-read, not accumulated |
| Exactly one inference flag | None needed — every claim here is the source's own stated fact about the mechanism (folder, steps, determinism), not an inference beyond it |
| One anchor, planted early, paid off late | B01 → B03, the month-end-prep folder (SKILL.md + reference), run identically in January and June |
| Both failure directions | B03: same file run twice → same result (positive); something left out of the file → not invented, not covered (negative) |
| No design judgment | B03 restates the source's Teardown "what it gets right / where it bites" as a determinism-and-boundary fact, with no verdict on whether month-end-prep is well designed |

## Deliberately not claimed

- **Not the specific steps of month-end-prep.** The source sheet's
  per-skill description was never filled in for this skill (see
  QUESTION.md); this reel does not invent a close checklist to cover the
  gap. It carries only the mechanism facts the source actually states.
- **Not a verdict on the skill's design.** The source's B03 Teardown beat
  framed the same facts as "what it gets right" / "where it bites" —
  trade-off language. Restated here as a plain determinism-and-boundary
  fact, judgment removed.
- **Not a claim that determinism implies correctness about the business.**
  Only that the same file, run twice, executes the same procedure.

## Handoff prompt (BHTF, read aloud then discussed)

> "Read the month-end-prep skill. Before you do anything, walk me through
> each step you're about to take, and point to the line in the file it
> comes from."

Why it's worth running: it turns the mechanism claim into something the
viewer can check directly — if Claude's stated steps don't trace back to
actual lines in the file, that's the skill's stated boundary being crossed.

---
**GATE P — signed:** ______________________  (human)
