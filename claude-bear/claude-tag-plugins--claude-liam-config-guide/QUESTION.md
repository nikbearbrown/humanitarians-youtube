# QUESTION.md

**Question:** When you ask Claude to change a setting, where does the answer
actually come from — one settings file, or something else?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-config-guide`), a fully-built Teardown
reel under
`anthropics/claude-tag-plugins/youtube/claude-liam-config-guide/` that walks
the `config-guide` Skill (an index skill covering agents, agent scopes,
identity profiles, and presets/connections/repos/instructions). Not a live
viewer submission.

**Name usable:** N/A (no submitter).
