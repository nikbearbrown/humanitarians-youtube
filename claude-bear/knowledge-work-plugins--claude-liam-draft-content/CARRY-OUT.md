# CARRY-OUT — knowledge-work-plugins--claude-liam-draft-content

**Carry-out line (written before scripting, per CARRY-OUT LAW):**

> A Claude skill isn't a new writing talent — it's one file doing one job.
> Same input, same output, every time you run it, and only for the content
> it's actually scoped to write.

**Test:** repeated alone in a meeting next week, still true — a skill file
is not a general creative upgrade to the model, it is a scoped, repeatable
spec.

**Wrong guess it defeats:** that installing/running a named skill like
`draft-content` gives Claude some new general writing ability or creative
talent it didn't have before. It doesn't — Claude already writes; the skill
is a file that scopes *how* it writes for named channels (blog, social,
email, landing page, press release, case study) and *when* to reach for it.

**Distinction compressed:** "new ability" vs. "one file, one job, same
input/output, bounded scope" — not the topic (marketing content drafting)
but the mechanism (what a Claude skill actually is and is not).
