# QUESTION.md

**Question:** Ask Claude for a complex dashboard as a claude.ai artifact — isn't
Claude just hand-writing one HTML file, the same as it would for a simple page?

**Asked by:** framed by this redo, not a real inbound viewer question —
`web-artifacts-builder` is an Anthropic skill (a `SKILL.md` Claude reads before it
works), and the natural newcomer question when shown "Claude builds a shareable
dashboard artifact" is "isn't that still just one file Claude writes by hand?"

**Name usable:** N/A (no real asker; redo of `claude-liam-web-artifacts-builder`, a
Teardown-register skill explainer built 2026-07-18 from
`anthropics/skills/skills/web-artifacts-builder/SKILL.md` — the source reel's own
`beats[*].narration_text` carries the facts instead).

**Mode:** redo. Source: `anthropics/skills/youtube/claude-liam-web-artifacts-builder/beat_sheet.json`
(7 beats: B00, B01, B02, B05, BVDT, BHTF, BOUT — Teardown register, no SCRIPT.md — its
`beats[*].narration_text` is the locked narration per the redo contract).
