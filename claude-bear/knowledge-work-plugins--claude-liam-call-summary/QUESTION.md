# QUESTION.md

**Question:** "Claude, Call Summary." — what does the `call-summary` Anthropic
skill actually do, and how does it do it?

**Asked by:** n/a — redo-mode reel. Source is a fully-built Teardown reel
(`anthropics/knowledge-work-plugins/youtube/claude-liam-call-summary`) that
walks through the `call-summary` skill (folder: `sales/skills/call-summary/`).
This build re-registers that same question and those same facts to the Plain
register for @HumanitariansAI, per `hai-simple`'s redo contract.

**Name use:** n/a — no individual is named or quoted.
