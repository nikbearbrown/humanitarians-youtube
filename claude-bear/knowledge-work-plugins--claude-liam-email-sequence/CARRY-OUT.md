# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Ask it for an email, and email-sequence hands back the whole sequence —
> copy, timing, branches, exit conditions — built the same way every time.

**Wrong guess it defeats:** that the skill just writes the text of one
email (B00's hesitant-writer correction: "email" → "sequence" — the naive
framing asks Claude to write one email; the corrected question asks it to
write the sequence, which is what the rest of the reel shows: a skill is a
folder Claude reads before acting, the SKILL.md is the whole instruction
set, the Steps run in a fixed order, and the actual job is designing a full
multi-email flow — copy for every message, timing between sends, branching
logic for opens and clicks, exit conditions, and benchmarks — not a single
message).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters (one ask in, a whole flow out,
deterministically) — not a summary of the whole topic.
