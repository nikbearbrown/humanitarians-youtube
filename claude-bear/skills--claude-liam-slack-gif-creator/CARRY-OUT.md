# CARRY-OUT — skills--claude-liam-slack-gif-creator

**The line (BCRY, written before any other narration):**

> Slack GIF Creator hands you the exact numbers and the assembly toolkit.
> The drawing is still yours — and it isn't done until the validator says
> so.

**The wrong guess it defeats:** that asking Claude for a Slack GIF gets you
a finished animation the way a template library would — call "bounce," get
`bounce.gif`. B00's hesitant-writer correction ("make it" → "assemble it")
carries the top-level version of that guess; the body's dedicated
WRONG-GUESS/BREAK beat (NB03/NB04) carries the specific form of it — that
there is a `bounce()` function to call — and breaks it with the actual
division of labor: easing hands back a curve, GIFBuilder assembles and
quantizes frames, and the per-frame PIL drawing is code the user (via
Claude) writes.

**Test:** if someone repeats only this line next week — "it hands you the
spec and the toolkit, you still draw it, and the validator has the last
word" — it is still true, and it compresses the distinction that matters
(knowledge + utilities vs. a finished product), not the topic (Slack
GIFs).
