# QUESTION.md

**Question:** When Claude chases down an overdue invoice, is it improvising
case by case — or following a fixed written procedure?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-invoice-chase`), a fully-built Teardown
reel under
`anthropics/knowledge-work-plugins/youtube/claude-liam-invoice-chase/`
that walks the Anthropic `invoice-chase` small-business Skill. Not a live
viewer submission.

**Name usable:** N/A (no submitter).

**Source-fidelity note:** the source `beat_sheet.json`'s narration carries
several literal, never-filled template placeholders (a bare `>` in B00, B03,
BVDT, and BHTF where invoice-chase-specific detail should be) — the source's
own SKILL.md is not present on this machine (its recorded path is
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/knowledge-work-
plugins/small-business/skills/invoice-chase/SKILL.md`, a different machine).
The source's *general* facts about Anthropic Skills — a Skill is a folder
Claude reads before acting, `SKILL.md` is the whole instruction set, Claude
executes its steps in order, same input produces same output, and the Skill
can only do what its file specifies — are confirmed, current, and generic to
every Skill, so this redo keeps them. The specific literal step list inside
`invoice-chase`'s own `SKILL.md` is not reconstructed or invented; NB02
carries an explicit ONE-FLAG line saying so, per ONE-FLAG LAW.
