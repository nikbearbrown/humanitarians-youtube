# QUESTION.md

**Question:** Does Claude use one all-purpose PDF library, or does the `pdf` skill
route to a different tool depending on the job?

**Asked by:** framed by this redo, not a real inbound viewer question — `pdf` is an
Anthropic skill (a `SKILL.md` Claude reads before it works), and the natural newcomer
question when shown a PDF task is "why not just always use the same PDF library?"

**Name usable:** N/A (no real asker; redo of `claude-liam-pdf`, a Teardown-register
skill explainer built 2026-07-18 from `anthropics/skills/skills/pdf/SKILL.md` +
`FORMS.md` + `REFERENCE.md` — the source reel's own `beats[*].narration_text` carries
the facts instead).

**Mode:** redo. Source: `anthropics/skills/youtube/claude-liam-pdf/beat_sheet.json`
(7 beats: B00, B01, B02, B05, BVDT, BHTF, BOUT — Teardown register, no SCRIPT.md — its
`beats[*].narration_text` is the locked narration per the redo contract).
