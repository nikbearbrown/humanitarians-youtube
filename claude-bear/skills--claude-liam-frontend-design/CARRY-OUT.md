# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude doesn't design well by guessing — it designs well by drafting a
> plan, then checking whether that plan is actually different from the
> default. Skip the check, and the default comes back.

**Wrong guess it defeats:** that Claude will just guess at something that
looks good (B00's hesitant-writer correction: "guess" → "plan" — the naive
framing asks whether Claude will just guess its way to a good-looking page;
the corrected question asks whether it plans first, which is what the rest
of the reel shows: a required design plan — color, type, layout, signature —
followed by a critique step that checks the plan against Claude's own
generic defaults before any CSS gets written).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole skill.
