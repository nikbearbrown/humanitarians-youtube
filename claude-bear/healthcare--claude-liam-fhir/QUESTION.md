# QUESTION.md

**Question:** What does Claude's `fhir` skill actually do — does connecting Claude to a
hospital's records mean it reads and diagnoses the patient?

**Asked by:** framed by this redo, not a real inbound viewer question — `fhir` is an
Anthropic healthcare-book skill (a `SKILL.md` Claude reads before it works), and the
natural newcomer question when shown that name is "if Claude connects to a live EHR,
does it interpret what it finds, or just pull the data out in a structured form?"

**Name usable:** N/A (no real asker; redo of `claude-liam-fhir`, a Teardown-register
skill explainer built 2026-08-25 from
`/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/healthcare/plugins/healthcare/skills/fhir/SKILL.md`).

**Mode:** redo. Source: `anthropics/healthcare/youtube/claude-liam-fhir/beat_sheet.json`
(7 beats, Teardown register, no SCRIPT.md — its `beats[*].narration_text` is the locked
narration per the redo contract, same disposition as the `healthcare--claude-liam-doc-extract`
sibling redo).
