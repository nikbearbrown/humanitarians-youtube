# CARRY-OUT.md — written before the body (CARRY-OUT LAW)

**Carry-out sentence:**

> Inside the LBO-model skill, Claude fills in a template's formulas and
> validates them — it doesn't design the deal itself. That's why it works
> on whatever template you hand it, and never beyond what that template
> specifies.

**Test:** if someone repeats only this in a meeting next week, is it still
true? Yes — it names the mechanism (fills and validates, doesn't design)
and its one consequence (works on any template, bounded by that template)
without leaning on any detail specific to `lbo-model` beyond what the
source actually states.

**Wrong guess it defeats:** that the skill builds the LBO model's
structure and deal assumptions the way an analyst would. The carry-out
states the opposite directly: it completes and checks a template that
already exists, on whichever layout it's handed.

**Signed:** GATE C — SIGNED (this file), 2026-09-02.
