# QUESTION.md

**Question:** When you ask Claude to build an MCP server, what actually
happens first — does it start writing server code, or does something else
have to get decided before any code exists?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-build-mcp-server`), a fully-built Teardown
reel under
`anthropics/claude-plugins-official/youtube/claude-liam-build-mcp-server/`
that walks the Build MCP Server Claude Code plugin-dev Skill (a
discovery-and-routing skill). Not a live viewer submission.

**Name usable:** N/A (no submitter).
