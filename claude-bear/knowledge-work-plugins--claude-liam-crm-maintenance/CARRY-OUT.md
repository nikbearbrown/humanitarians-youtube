# CARRY-OUT — knowledge-work-plugins--claude-liam-crm-maintenance

**Wrong guess it defeats:** "Claude, crm maintenance" sounds like an ongoing job —
Claude quietly keeping the CRM tidy on its own, in the background, all the time. It
doesn't work that way; the skill is a bounded specification, run once per request.

**Carry-out line (written first, before any other narration):**

> "Maintenance" here isn't an ongoing job Claude runs by itself. It's one spec-bound
> check — the same steps, the same result, every time you ask.

**Survives-repetition test:** someone repeating only this sentence next week is still
describing what a Claude Skill named `crm-maintenance` does correctly — it compresses
the distinction that matters (bounded, request-triggered, repeatable — not a standing
autonomous background process) rather than summarizing the topic, and it makes no claim
about a domain-specific detail this build could not recover from its source (see
QUESTION.md).
