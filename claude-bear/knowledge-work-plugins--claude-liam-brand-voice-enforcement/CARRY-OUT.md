# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> brand-voice-enforcement doesn't know your brand's voice — it checks your
> writing against whatever its SKILL.md actually lists. Same input, same
> output, every time. The limit is exactly what the file says, nothing more.

**Wrong guess it defeats:** that Claude "already knows" a brand's voice —
that a skill named brand-voice-enforcement works by some trained sense of
tone (B00's hesitant-writer correction: "know" → "check" — the naive framing
asks whether Claude already knows the brand's voice; the corrected question
asks whether it checks it, which is what the rest of the reel shows: a skill
is a folder Claude reads before acting, the SKILL.md is the whole
instruction set, the steps run in a fixed order, and the actual job is
comparing a draft against whatever rules the file lists — nothing it wasn't
told).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
