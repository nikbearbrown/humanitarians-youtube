# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> competitive-brief always runs the same three moves — research, compare,
> flag the gaps — in the same order, every time. It only ever does what the
> SKILL.md's steps say to do.

**Wrong guess it defeats:** that Claude has some hidden, proprietary
algorithm for researching competitors (B00's hesitant-writer correction:
"algorithm" → "checklist" — the naive framing asks about a special
algorithm; the corrected question asks about a checklist, which is what the
rest of the reel shows: the skill is a plain-language file with a Steps
section, executed in order, and it never does more than those steps
specify).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
