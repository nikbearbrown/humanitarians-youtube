# BUILD-LOG — launch-your-agent--claude-liam-launch-your-agent

## 2026-09-04 — review cut, DONE

Redo-mode build (`mode: "redo"`) of
`anthropics/launch-your-agent/youtube/claude-liam-launch-your-agent/beat_sheet.json`
(a Teardown skill-teardown walkthrough of the Anthropic `launch-your-agent`
Claude Skill — helps a technical founder build on Claude Managed Agents;
already fully built, no SCRIPT.md; source `beats[*].narration_text` served
as the locked script). Built entirely fresh this invocation — only
SUBJECT.json existed on pickup.

Question, facts, and full body argument carried over unchanged: a skill is
a folder Claude reads before it acts, and launch-your-agent's SKILL.md is
the instruction set; the pipeline (its Steps section) is: find out what
the founder wants to build, scope a v0, launch it into their account,
grade the result, iterate, and — if it should run on a clock — put it on
a scheduled deployment, with anything bigger laid out as v1/v2; execution
is linear, no branching unless a step says so; because the skill is a
fixed specification rather than open-ended judgment, it produces the same
output for the same input every run, and its edge is that Claude only
does what the SKILL.md actually specifies. B00 replaced the source's
`ClaudeComposerAsk` typed-ask cold open with `BrutalistHesitantWriter`
(WRITER LAW: "improvises" → "follows fixed steps" — the newcomer's wrong
guess that telling Claude to launch an agent means Claude improvises the
whole build, corrected toward the actual mechanism: it follows the
skill's fixed sequence of steps). Register re-registered Teardown→Plain:
the source's B03 "gets it right / where it bites" framing was stripped to
a plain mechanism-and-consequence description (same input/same output,
outside-the-spec boundary) rather than kept as a Teardown verdict, per the
NO JUDGMENT register check. BVDT's verdict facts (same input → same
output every run; limited to what the SKILL.md specifies) were merged
into the single BCRY carry-out sentence rather than kept as a separate
bulleted artifact card, per CARRY-OUT LAW — and BCRY was written to
resolve B00's specific wrong guess (a v0 launched into the founder's
account, not a finished product, with anything bigger earned through
grading and iteration) rather than restate the same fixed-spec fact NB03
already covers. Close re-skinned to @HumanitariansAI (`OutroSeries`).

**Truncation repair, not a new fact.** The source `beat_sheet.json`'s
narration and prop strings were generated by a batch builder that
truncated several fields mid-word at a fixed character count (B00's
narration: "...a piece of their product, a p"; BVDT's artifact lines:
"...an internal w"; BHTF's prompt: "...on claude managed ag."). This redo
restores the full sentences using the one place the source states the
complete fact without truncation — B00's own `narration_text` — and adds
no fact beyond what that sentence already specifies. Full accounting in
SCRIPT.md's "Truncation note."

**Beat count discipline:** source is 7 beats (B00 composer-ask + B01/B02
anatomy/pipeline + B03 design tell + BVDT verdict + BHTF your-turn + BOUT
outro). This redo kept the same 7-beat shape: B00 carries the wrong-guess
pedagogy per WRITER LAW instead of a dedicated beat; B01→NB01, B02→NB02,
B03→NB03 kept as one beat each; BVDT folded into BCRY; BHTF kept, with
the source's prompt restored (not altered) per the truncation-repair note
above; BOUT kept. Full audit in SCRIPT.md's "Beat-count note (redo)"
section.

No source beat was ai-video-prompt, pantry, or a human-drop slot — the
source's final build was already entirely REMOTION (`ClaudeComposerAsk` /
`SkillTeardownAnatomy` / `SkillTeardownPipeline` / `SkillTeardownMechanism`
/ `ClaudeVerdictArtifact`), so NO-GENAI/NO-PANTRY LAW required no
substitution beyond B00's mandated cold-open swap.

All 3 GRAPHIC beats (NB01–NB03) built on the shared generic "chip row"
Manim template (`scenes.py`/`render_scenes.py`) copied verbatim from the
`claude-plugins-official--claude-liam-agent-development` sibling, adapted
with launch-your-agent-specific labels.

**B00 TIMING LAW — settings reused from the already-fixed sibling
pattern, verified rather than assumed.** Used the proven-safe parameters
from the `claude-plugins-official--claude-liam-agent-development` sibling
(42ms/char, 4% mistakeRate, 2% hesitateWithin, 8% hesitateBetween, jitter
26, lead_silence_s 1.0) rather than the longer/faster/noisier settings
that caused that sibling's first-attempt TIMING LAW failure. Text: "Tell
Claude to / launch my agent / and it improvises, / right?" (58 forward
characters, comparable length to the sibling's fixed text). Kokoro audio
measured 11.31s (vs. the sibling's 10.09s) — narration alone already
exceeds the ≥9s floor before the 1.0s lead silence is added. Verified by
frame pulls, not assumed: at t≈4.5s "improvises" sits doomed in
terracotta; at the clip's last frame (11.3s) the full corrected question
"Tell Claude to launch my agent and it follows fixed steps, right?" is
completely typed and settled, with margin to spare.

Audio generated fresh (`generate_audio_kokoro.py`, all 7 beats, free/local,
`am_onyx`, single pass, no re-generation needed); B00/BCRY/BHTF/BOUT
rendered via `remotion_scenes.py` (foreground; the run exceeded the
tool's 120s timeout and was moved to background by the harness
automatically — blocked on it via `TaskOutput` before proceeding, per the
COMPLETION LAW's foreground-render rule, never treating a backgrounded
render as "handled" without waiting on it); NB01–NB03 rendered via
`render_scenes.py`. `type_check.py` (GATE T) passed **PASS, 0 FAILs** on
the first run — no defect to fix.

Compiled:

```
python3 runtime/scripts/compile.py <REEL_DIR>
```

Result: `launch-your-agent--claude-liam-launch-your-agent.mp4`, 7/7 beats
filled real (no slate), 97.8s, 3840×2160 (native 4K — `compile.py`'s 4K
LAW).

**Gates:**
- content-check: PASS (7 beats, no violations)
- frame-check: PASS (3840×2160, 7 beats, no violations)
- lane-check: PASS (no lane violations, cut=master)
- GATE T: PASS, 0 FAILs
- GATE AUDIO: PASS — mean_volume **-24.1 dB** (ffmpeg volumedetect), max -2.7 dB
- ffprobe: video 3840×2160 h264, audio (aac) present, duration 97.76s; mp4
  mtime (1788526777) newer than beat_sheet.json mtime (1788526639)
- Gate V (visual): pulled frames every ~6s across the full runtime plus
  targeted checks of B00 (t≈2.2s mid-type, t≈4.5s "improvises" doomed in
  terracotta, last frame at 11.3s fully corrected and settled), NB01
  ("A SKILL IS A FOLDER" chip row, arrows + accent clean), NB02 ("HOW THE
  SKILL RUNS", verified the bold "launch + grade" chip has no glyph
  overlap via a zoomed crop), NB03 ("A FIXED SPEC", legible), BCRY
  (carry-out sentence + sparkline read clean, exact quote), BHTF (correct
  topic/title/@HumanitariansAI handle, paste-ready prompt text legible),
  and BOUT (OutroSeries: correct eyebrow "LAUNCH YOUR AGENT ·
  @HumanitariansAI", correct title restate, crimson underline, no
  truncation). No blockers.
- B00 TIMING LAW: `actual_duration_s` 11.31s (≥8s requirement met, and
  clear margin above the sibling's 10.09s); the "improvises" → "follows
  fixed steps" correction lands on screen by t≈4.5s and the full corrected
  question stays legible for the remainder of the clip.

Metadata file written: `launch-your-agent--claude-liam-launch-your-agent.md`
(channel @HumanitariansAI, **Playlist: Claude Basics**). Per
`playlists.json`, SUBJECT.json's family (`launch-your-agent`) matches none
of the map's family-prefix keys (no key is a prefix of `launch-your-agent`),
so resolution fell through to the `hai-simple` skill-key fallback (`str.
startswith` match: `"hai-simple".startswith("hai-simple")`), which resolves
to "Claude Basics" — a more specific, real playlist than the `_default`
last resort, and consistent with hai-simple SKILL.md's own framing of the
series as Claude-basics content. Direct code link per DELIVERY CONTRACT
format included.

**Status: review cut DONE.** Passed every Phase-3 gate.

## 2026-09-04 — Phase 4, DELIVERED

Master was already born native 3840×2160 (compile.py's 4K LAW), so copied
directly to `launch-your-agent--claude-liam-launch-your-agent-4k.mp4`
rather than re-rendering at a higher resolution.

```
python3 skills/make/hai-simple/loop/deliver.py <REEL_DIR> --push
```

Staged `DELIVERY/launch-your-agent--claude-liam-launch-your-agent/` (4K
master + description) for the Drive sync. Committed to
`claude-bear/launch-your-agent--claude-liam-launch-your-agent/`
(README.md = description, beat_sheet.json, SCRIPT.md, SUBJECT.json,
CARRY-OUT.md, QUESTION.md, BUILD-LOG.md — no mp3/mp4) as commit
`6511c0f2`, pushed clean (no rebase conflicts).

**Status: DELIVERED.**
