# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Claude skill isn't an app running on its own — it's a file Claude reads,
> so it does exactly what that file says, the same way, every time, and
> nothing beyond what's written.

**Wrong guess it defeats:** that a named skill like customer-pulse is a piece
of software Claude has installed and switches on (B00's hesitant-writer
correction: "app" → "file" — the naive framing asks if opening
customer-pulse's *app* tells Claude what to do; the corrected question asks
about opening its *file*, which is what the rest of the reel shows: the
skill is a folder holding one plain-language document, read top to bottom,
executed one step at a time, with no capability beyond what that document
specifies).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of what customer-pulse
happens to check.
