# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude isn't writing three different lessons — it's tiering one shared
> lesson three ways, from a single source file, so the three versions can't
> drift apart.

**Wrong guess it defeats:** that differentiating a lesson for three
proficiency levels means drafting three separate, independent lessons
(B00's hesitant-writer correction: "separate" → "one shared version" — the
naive framing asks if Claude writes the tiers separately; the corrected
question asks whether it writes one shared version instead, which is what
the rest of the reel shows: a named skill folder, a linear
read/execute/return pipeline, and the specific design fact that all three
tier documents and the teacher's plan render from one shared material
source, so nothing can drift between them).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
