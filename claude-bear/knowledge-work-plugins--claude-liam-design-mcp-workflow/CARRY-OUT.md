# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Claude skill isn't a new ability — it's one file doing one job. Same
> input, same output, every time you run it, and only for what that file
> actually covers.

**Wrong guess it defeats:** that a skill hands Claude some new general-purpose
ability (B00's hesitant-writer correction: "ability" → "file" — the naive
framing asks whether Claude "got a new ability," the corrected question asks
what the skill's file actually does, which is what the rest of the reel
shows: the SKILL.md is one file Claude reads before acting, its steps run in
a fixed order, and this particular one is scoped to exactly one decision —
Zoom MCP tools vs. Zoom's REST API — nothing outside that).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
