# QUESTION — claude-quickstarts--macos-computer-use-coordinate-roundtrip

**Question (from the source sheet, carried unchanged):** Two Resolutions, One Click —
the macOS Coordinate Roundtrip. Your MacBook's Retina screenshot is much bigger than
what the Claude API actually looks at. Claude clicks somewhere on the resized copy —
where does that land on your real screen, and how do you compute it?

**Asked by:** no named human asker — this is a `mode: "redo"` build. The source is
`anthropics/claude-quickstarts/youtube/macos-computer-use-coordinate-roundtrip/beat_sheet.json`,
a Teardown-register scaffold (4/8 beats claimed as MANIM in the sheet, but no
`manim/` directory exists on disk — the fills were never actually rendered) built
from `computer-use-best-practices/computer_use/image.py`.

**Name usable:** n/a (no human asker).
