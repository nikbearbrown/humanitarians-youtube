# QUESTION.md

**Question:** What does Claude's `icd10-cm-skill` skill actually do?

**Asked by:** framed by this redo, not a real inbound viewer question — `icd10-cm-skill`
is an Anthropic healthcare-book skill (a `SKILL.md` Claude reads before it works), and
the natural newcomer question when shown that name is "does this diagnose the patient,
or just turn an already-written diagnosis into a billing code?"

**Name usable:** N/A (no real asker; redo of `claude-liam-icd10-cm-skill`, a Teardown-
register skill explainer built 2026-08-25 from a source path this machine does not have
— `/Users/bear/Documents/CoWork/bear-textbooks/books/anthropics/healthcare/plugins/healthcare/skills/icd10-cm/SKILL.md`;
the source reel's own `beats[*].narration_text` carries the facts instead).

**Mode:** redo. Source: `anthropics/healthcare/youtube/claude-liam-icd10-cm-skill/beat_sheet.json`
(7 beats, Teardown register, no SCRIPT.md — its `beats[*].narration_text` is the locked
narration per the redo contract, same disposition as the `doc-extract` / `fhir` /
`fraud-detection` healthcare siblings).
