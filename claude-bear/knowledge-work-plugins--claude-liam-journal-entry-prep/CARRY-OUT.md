# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> Claude doesn't guess the accounting — it runs the same written steps on
> the numbers you give it, every time. What it covers is exactly what that
> file names, and nothing past it.

**Wrong guess it defeats:** that Claude prepares journal entries by drawing
on trained accounting judgment (B00's hesitant-writer correction: "training"
→ "a skill file" — the naive framing asks if Claude preps journal entries
using training; the corrected question asks whether it does so using a
skill file, which is what the rest of the reel shows: a folder Claude reads
before it acts, whose file names a specific list of transaction types —
accruals, prepaid amortization, depreciation, payroll, revenue recognition —
and nothing outside that list).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of what
journal-entry-prep does.
