# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A skill runs the same steps on every request — read the file, execute in
> order, return the result. It only knows what's written in that file,
> nothing more.

**Wrong guess it defeats:** that a finished competitive-analysis deck means
Claude judged the market itself (B00's hesitant-writer correction: "judged"
→ "processed" — the naive framing assumes independent evaluation; the
corrected question asks whether the skill instead processes a fixed
procedure, which is what the rest of the reel shows: the skill is a folder
Claude reads before it works, the pipeline is a linear read → execute →
return with no branching unless a step says so, and the whole thing only
ever answers within the written scope of that one file).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
