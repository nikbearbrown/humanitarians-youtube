# QUESTION.md

**Question:** When Claude drafts or evaluates an architecture decision
record, is it using its own judgment about good design — or something else
entirely, like a fixed instruction file it reads first?

**Who asked / where:** Redo-mode reel. Source question carried over from the
locked source script (`claude-liam-architecture`), a fully-built Teardown
reel under
`anthropics/knowledge-work-plugins/youtube/claude-liam-architecture/` that
walks the Anthropic `architecture` Claude skill (create or evaluate an ADR).
Not a live viewer submission.

**Name usable:** N/A (no submitter).
