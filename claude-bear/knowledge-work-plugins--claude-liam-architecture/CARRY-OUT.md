# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A skill runs the same steps every time you call it — never a step beyond
> what its file wrote down.

**Wrong guess it defeats:** that Claude's architecture calls come from its
own judgment about good design (B00's hesitant-writer correction: "judgment"
→ "its SKILL.md" — the naive framing asks if judgment writes the call, the
corrected question asks whether the SKILL.md does, which is what the rest of
the reel shows: the skill is a folder Claude reads before it works, the
pipeline reads and executes its Steps section in order, and the whole thing
covers exactly one job — ADR creation and evaluation — and nothing outside
that file's script).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
