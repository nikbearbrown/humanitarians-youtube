# BUILD-LOG — launch-your-agent--claude-liam-wrap-up

## 2026-09-04 — review cut, DONE

Redo-mode build (`mode: "redo"`) of
`anthropics/launch-your-agent/youtube/claude-liam-wrap-up/beat_sheet.json`
(a Teardown skill-teardown walkthrough of the Anthropic `wrap-up` Claude
Skill — closes out, or revisits, a Claude Managed Agent build; already
fully built, no SCRIPT.md; source `beats[*].narration_text` served as the
locked script). Built entirely fresh this invocation — only SUBJECT.json
existed on pickup. Followed the proven pattern from the family sibling
`launch-your-agent--claude-liam-launch-your-agent` (same source shape:
composer-ask cold open + anatomy + pipeline + design-tell + verdict +
your-turn + outro), reusing its scenes.py chip-row mechanism verbatim
(only labels changed) and its B00 timing settings.

Question, facts, and full body argument carried over unchanged: a skill
is a folder Claude reads before it acts, and wrap-up's SKILL.md is the
instruction set; the pipeline refreshes the overview page, recaps every
primitive the founder now owns, shows the run log and live status,
suggests one or two tailored upgrades, then sweeps hygiene (sessions
archived, key only in .env, no literal dates in deployment kickoffs);
wrap-up is idempotent — running it again doesn't rebuild or duplicate
anything, it only refreshes the same page and tables, same input → same
output every run; its edge is that it only recaps and suggests, it
doesn't build the upgrades it names and only checks what the SKILL.md
specifies. B00 replaced the source's `ClaudeComposerAsk` typed-ask cold
open with `BrutalistHesitantWriter` (WRITER LAW: "closes" → "recaps" —
the newcomer's wrong guess that wrapping up an agent shuts the project
down for good, corrected toward the actual mechanism: it hands back a
recap you can re-run any time). Register re-registered Teardown→Plain:
the source's B03 design-tell framing ("here is the design tell... an
interesting constraint... a deliberate trade-off") was stripped to a
plain mechanism-and-consequence description (idempotent → same
input/output, recap-not-build boundary) rather than kept as a Teardown
verdict, per the NO JUDGMENT register check. BVDT's verdict facts (same
input → same output every run; limited to what the SKILL.md specifies)
were merged into the single BCRY carry-out sentence rather than kept as
a separate bulleted artifact card, per CARRY-OUT LAW — and BCRY was
written to resolve B00's specific wrong guess (a re-runnable recap, not
a shutdown) rather than restate the same fixed-spec fact NB03 already
covers. Close re-skinned to @HumanitariansAI (`OutroSeries`).

**Truncation repair, not a new fact.** The source `beat_sheet.json`'s
narration strings were generated by a batch builder that truncated
several fields mid-list at a fixed character count (B03's design-tell
body ends "...Idempotent: running it again just refreshes the page and
tables.." with a stray double period and no lead-in; BVDT's narration
ends "...refresh the overview page,." then jumps straight to "Same
input, same output"; BHTF's prompt ends "...refresh the overview
page,." then jumps to "Read the wrap-up skill"). This redo restores the
full sentences using the one place the source states the complete fact
without truncation — B00's own `narration_text`, which spells out the
skill's full job ("Close out (or revisit) a Claude Managed Agent build —
refresh the overview page, recap every primitive the founder now owns,
show the run log and live status, suggest 1–2 tailored next upgrades,
and sweep hygiene (sessions archived, key only in .env, no literal dates
in deployment kickoffs).") — no fact is added beyond what that sentence
already specifies, only the mid-list cutoffs are repaired. Full
accounting in SCRIPT.md's "Truncation note."

**Beat count discipline:** source is 7 beats (B00 composer-ask + B01/B02
anatomy/pipeline + B03 design tell + BVDT verdict + BHTF your-turn +
BOUT outro). This redo kept the same 7-beat shape: B00 carries the
wrong-guess pedagogy per WRITER LAW instead of a dedicated beat;
B01→NB01, B02→NB02, B03→NB03 kept as one beat each (NB02 restores the
concrete pipeline steps the source's own B02 left abstract, using
B00's untruncated list); BVDT folded into BCRY; BHTF kept, with the
source's prompt restored (not altered) per the truncation-repair note
above; BOUT kept. Full audit in SCRIPT.md's "Beat-count note (redo)"
section.

No source beat was ai-video-prompt, pantry, or a human-drop slot — the
source's final build was already entirely REMOTION (`ClaudeComposerAsk`
/ `SkillTeardownAnatomy` / `SkillTeardownPipeline` /
`SkillTeardownMechanism` / `ClaudeVerdictArtifact`), so NO-GENAI/
NO-PANTRY LAW required no substitution beyond B00's mandated cold-open
swap.

All 3 GRAPHIC beats (NB01–NB03) built on the shared generic "chip row"
Manim template (`scenes.py`/`render_scenes.py`) copied verbatim from the
`launch-your-agent--claude-liam-launch-your-agent` sibling, adapted with
wrap-up-specific labels.

**B00 TIMING LAW — settings reused from the already-fixed sibling
pattern, verified rather than assumed.** Used the proven-safe parameters
from the `launch-your-agent--claude-liam-launch-your-agent` /
`claude-plugins-official--claude-liam-agent-development` siblings (42ms/
char, 4% mistakeRate, 2% hesitateWithin, 8% hesitateBetween, jitter 26,
lead_silence_s 1.0). Text: "Tell Claude to / wrap up my agent / and it
closes / the project, right?" Kokoro audio measured 10.69s — narration
alone already exceeds the ≥9s floor before the 1.0s lead silence is
added; media/B00.mp4 rendered at 10.7s. Verified by frame pulls, not
assumed: at t≈4.5s "closes" sits doomed in terracotta; at the clip's
last frame the full corrected question "Tell Claude to wrap up my agent
and it recaps the project, right?" is completely typed and settled.

**GATE T iteration (one real defect, one cosmetic defect, both fixed
before the cut):**
1. First render: NB01 and NB02 chip text FAILed min-size §8.1 (19px /
   16px < the 20px floor) because their longer chip labels (e.g.
   "refresh + recap", "run log + status") pushed the font-size tier down
   from 26pt to 22pt and then forced further shrink-to-fit. Fixed by
   raising `scenes.py`'s font-size tiers (30/26/22 instead of 26/22/18)
   and shortening NB02's chip labels ("refresh, recap" / "log + status"
   / "upgrades, hygiene") to fit the 26pt tier with margin. Re-render,
   recompile, re-check: GATE T PASS.
2. Frame-QC (not caught by GATE T, caught by reading the actual pixels
   per COMPLETION LAW): NB03's bold accented chip label "recap, not
   build" rendered with the space between "not" and "build" collapsing
   to zero width in Manim's bold EB Garamond — "notbuild" read as one
   word. Confirmed by cropping the frame; the same bold-weight chip
   pattern elsewhere ("the program", "upgrades, hygiene") rendered
   correctly, isolating the defect to that specific word pair rather
   than the mechanism. Fixed by simplifying the label to "recap only"
   (two words, no adjacency collision) — re-rendered NB03 only,
   recompiled, re-verified the frame: clean, legible, GATE T still PASS.

Audio generated fresh (`generate_audio_kokoro.py`, all 7 beats, free/
local, `am_onyx`, single pass, no re-generation needed); B00/BCRY/BHTF/
BOUT rendered via `remotion_scenes.py` (foreground, ran to completion
inside the tool timeout); NB01–NB03 rendered via `render_scenes.py`
(foreground, re-run for NB03 only after the label fix — `[skip]` on the
untouched NB01/NB02 outputs). `type_check.py` (GATE T) FAILed once (2
FAILs, min-size), was fixed, and PASSed clean on the second run — 0
FAILs.

Compiled:

```
python3 runtime/scripts/compile.py <REEL_DIR>
```

Result: `launch-your-agent--claude-liam-wrap-up.mp4`, 7/7 beats filled
real (no slate), 95.5s, 3840×2160 (native 4K — `compile.py`'s 4K LAW).

**Gates:**
- content-check: PASS (7 beats, no violations)
- frame-check: PASS (3840×2160, 7 beats, no violations)
- lane-check: PASS (no lane violations, cut=master)
- GATE T: PASS, 0 FAILs (after the two-part fix above)
- GATE AUDIO: PASS — mean_volume **-24.1 dB** (ffmpeg volumedetect), max -2.9 dB
- ffprobe: video 3840×2160 h264, audio (aac) present, duration 95.54s; mp4
  mtime (1788528468) newer than beat_sheet.json mtime (1788528413)
- Gate V (visual): pulled frames every ~6s across the full runtime plus
  targeted checks of B00 (t≈4.5s "closes" doomed in terracotta, last
  frame at 10.7s fully corrected and settled to "recaps"), NB01 ("A
  SKILL IS A FOLDER" chip row, arrows + accent clean), NB02 ("WHAT THE
  RECAP COVERS", three chips legible, no overlap), NB03 ("IDEMPOTENT,
  NOT A REBUILD", re-verified "recap only" renders as two clearly
  spaced words after the label fix), BCRY (carry-out sentence + spark
  line read clean, exact quote), BHTF (correct topic/title/
  @HumanitariansAI handle, paste-ready prompt text legible), and BOUT
  (OutroSeries: correct eyebrow "WRAP-UP · @HumanitariansAI", correct
  title restate, crimson underline, no truncation). No blockers.
- B00 TIMING LAW: `actual_duration_s` 10.7s (≥8s requirement met); the
  "closes" → "recaps" correction lands on screen by t≈4.5s and the full
  corrected question stays legible for the remainder of the clip.

Metadata file written: `launch-your-agent--claude-liam-wrap-up.md`
(channel @HumanitariansAI, **Playlist: Claude Basics**). Per
`playlists.json`, SUBJECT.json's family (`launch-your-agent`) matches
none of the map's family-prefix keys, so resolution fell through to the
`hai-simple` skill-key fallback (`"hai-simple".startswith("hai-simple")`),
which resolves to "Claude Basics" — same resolution path as the
`launch-your-agent--claude-liam-launch-your-agent` sibling. Direct code
link per DELIVERY CONTRACT format included.

**Status: review cut DONE.** Passed every Phase-3 gate.
