# CARRY-OUT.md

**Carry-out line (BCRY narration):**
> A Claude skill isn't a hidden app — it's a plain-text file of instructions,
> so the same input produces the same report every time. Whatever that file
> doesn't say, Claude has to work out on its own.

**Wrong guess it defeats:** that a repeatable, domain-specific capability like
client-report means Claude has a built-in finance app or hidden logic for the
task (B00's hesitant-writer correction: "app" → "skill" — the naive framing
asks whether the client-report app understands finance; the corrected
question asks whether the skill does, which is what the rest of the reel
answers: a skill is a folder with one file, SKILL.md, that Claude reads and
executes step by step, and the file's coverage is the only thing that
repeats — anything it doesn't specify, Claude reasons past on its own).

**Test:** if someone repeats only this sentence next week, it is still true
and still the distinction that matters — not a summary of the whole topic.
