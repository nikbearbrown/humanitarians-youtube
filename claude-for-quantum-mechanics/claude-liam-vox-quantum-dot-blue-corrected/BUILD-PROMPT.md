# Build Prompt

From `books/`, build this folder with the `ai-explainer` workflow. Preserve the free `claude-liam` Kokoro `af_kore` channel, Teardown narration, Claude cold open only, native concept body, Your Turn, corrected title outro, and all fact-check limitations. Render 16:9, compile audio-first, probe the master, visually inspect sampled frames/contact sheet, fix every blocker or major defect, document QC, and never publish.
