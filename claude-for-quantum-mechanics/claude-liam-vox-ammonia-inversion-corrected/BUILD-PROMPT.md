# Build Prompt

From `books/`, run the `ai-explainer` build for this folder using free `claude-liam` Kokoro `af_kore`. Preserve the fact corrections, Claude cold open, native concept body, Your Turn calculation, title-restating outro, and no-publish rule. Render 16:9 audio-first, probe the master, visually inspect the contact sheet and sampled frames, fix all blocker/major defects, and write `QC-REPORT.md`.
