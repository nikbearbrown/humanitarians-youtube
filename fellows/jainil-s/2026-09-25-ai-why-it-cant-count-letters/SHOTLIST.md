# SHOTLIST — Why It Can't Count Letters

Typed work order. All nine beats are Remotion; no human media, no open slots.

| Beat | Act | Composition | What must be on screen | Accent (terracotta) |
|---|---|---|---|---|
| B00 | ASK | `ClaudeComposerAsk` | "Ciao, Liam"; the ask; `@HumanitariansAI`; `Fable 5` chip | send button |
| B01 | BLUF | `BrutalistHesitantWriter` | Two lines; "letters" typed, reconsidered, replaced with "chunks" | the doomed word |
| B02 | HERO | `TokenSplit` | `strawberry` as 10 character chips above, 3 token chips below; the r's marked | the r's + token borders |
| B03 | TURN | `TokenSplit` | Same word, leading space, **one** token chip | the single token |
| B04 | EVIDENCE | `FormACard` | Four real splits + "reproduce with tokenize_evidence.py" | — |
| B05 | LAND | `WantQuote` | Pull quote; spark "Fifty thousand chunks, not twenty-six letters." | quote mark |
| B06 | FIX | `FormACard` | Four remedies | — |
| B07 | HANDOFF | `ClaudeComposerAsk` | "Your turn."; a pasteable prompt | send button |
| B08 | OUTRO | `HaiTitleOutro` | Title restate + SUBSCRIBE + `@HumanitariansAI`. No mascot, no subline. | — |

**B02 and B03 are load-bearing** — the whole argument is the contrast between
the two rows, and then the fact that one space collapses it. The tokenizer
attribution in the caption must not be dropped.

**Segment titles ALL CAPS** per the published style.

**Declared sparse** (`qc.sparse: true`): B01, B02, B03, B04, B05, B06, B08.

**Shorts:** every composition has a `916` variant (`TokenSplit916` built for
this reel), so no beat is dropped for a missing portrait scene.
