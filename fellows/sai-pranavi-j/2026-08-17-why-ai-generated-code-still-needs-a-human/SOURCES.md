# SOURCES — Why AI-Generated Code Still Needs a Human Who Understands the System

## Status: no external sources required

This video teaches a reusable rubric (Trace / Consequence / Why) and
demonstrates it on a **generic, illustrative** code example (a hand-escaped
SQL insert vs. a parameterized-query fix) rather than reporting a real,
specific incident. There is no factual/statistical claim here that needs an
external citation — see `FACTCHECK.md` for the claim-by-claim review.

## What this reel does NOT claim

- Does not attribute the B02 worked example to any specific real codebase,
  company, or incident.
- Does not cite Project 29 (`mycroft`) or any of this fellow's other project
  work, even though a real, matching bug/fix exists there — that was
  deliberately set aside (see `FACTCHECK.md` → "No-fabrication note" and
  `BUILD-LOG.md`).
- Does not present the rubric as empirically validated research — it is an
  editorial framework for scrutinizing AI-suggested code changes.

## If a future cut sources the worked example from real code

Any revision that wants to swap in a real before/after diff must add a
`SOURCES.md` entry with the exact repo/file/line for both versions, and a
`FACTCHECK.md` pass verifying the claimed failure mode against that real
code — matching the standard this fellow's other two videos hold themselves
to (see `2026-07-26-recovering-the-silently-dropped-filings/SOURCES.md` and
`2026-07-27-how-facial-recognition-actually-works/SOURCES.md` for the
pattern).
