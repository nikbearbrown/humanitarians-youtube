# Build log

- 2026-08-30 — Scoped video from Project 29's continued Layer 1 hardening pass: the `identifySource()` CFTC-detection heuristic (`link.includes('commodity-futures') || title.includes('cftc')`) tested against every real, live CFTC Regulations item today and matched **0 of 12**. Chosen over the still-open source-misclassification bug and the Google News link-unwrapping task (confirmed a bigger scrape-based project, not a quick fix) because it has a clean, fully measured before/after across all 5 live feeds and a sharp general lesson: a safeguard never tested against real input isn't protecting anything.
- 2026-08-30 — `BEAT-SHEET.md`, `beat_sheet.json`, `FACTCHECK.md`, `SOURCES.md` authored and approved (Gate P). Both FACTCHECK open items resolved: the "tested live today" / "pulled live" framing confirmed sufficient as written (not a claim about the pipeline's entire historical run history); confirmed no downstream incident (a specific bad alert or report) is implied anywhere in the script — the reel stays scoped to classifier behavior on a live test.
- 2026-08-30 — Voice: Bella (`af_bella`) — locked for this fellow's whole report series, unchanged from the 2 prior episodes.
- 2026-08-30 — Generated Kokoro audio for all 9 beats: B00 silent title card, B01-B08 narrated via `generate_audio_kokoro.py`. Measured `actual_duration_s` for every beat from real `ffprobe` output: 4.05/18.98/11.38/19.32/19.66/29.42/13.94/10.56/6.26s — total runtime 133.57s.
- 2026-08-30 — Authored `scenes.py`: 9 Manim scenes (B00-B08), same PALETTE/MONO/`fit()`/`panel()`/`clear_of_divider()`/`box_around()` helpers as this fellow's sibling reel `2026-08-30-the-update-that-almost-lied-about-what-it-sent` (part 2 of the same Layer-1-hardening pair) for house-style consistency. Every code/query string on screen (B02/B03/B04/B06) is quoted verbatim from `B2-VERIFICATION.md` and the pre-fix/post-fix `workflow.dev.json` (commit `d59fbd5`) — nothing paraphrased. B04 (real filing vs. condition) is the reel's side-by-side beat, using `clear_of_divider()` verified against real measured Manim object bounds.
- 2026-08-30 — Added `SHOTLIST.md` and `PROMPTS.md` (GATE F paperwork set; this reel needs no pantry assets — all 9 beats are self-contained Manim).
- 2026-08-30 — GATE B (`manim_layout_audit.py`) on all 9 scenes: 4 snapshots inspected, **0 errors, 0 warnings** (`layout_audit.md`).
- 2026-08-30 — First full 4K render (`./art run`): 9/9 beats compiled real, master + review cut written (133.55s, `qc-sheet.png`). GATE V on the true clean 4K master (`_qc/REPORT.md`): **0 BLOCKER, 0 MAJOR** across all 18 sampled frames.
- GATES CLOSED — plan, fact-check, narration, audio lock, previz. See `beat_sheet.json` → `metadata.gates` for the dated record.
- NOT AUTHORIZED — Publishing.

## 2026-08-31 — 9:16 Shorts derivative built (`short/`)

- 2026-08-31 — `short/beat_sheet.json` and `short/scenes.py` were already scaffolded (via `runtime/scripts/shorts.py`) per THE SHORTS LAW: at 133.55s this reel is **under** the 180s Shorts cap, so the whole reel reformats 16:9→9:16 as-is — **0 beats dropped**, every beat's mp3 reused unchanged (symlinked from the parent's `mp3/`), plus a silent branded endcard (`media/END.png` + `mp3/beat-END.mp3`, 4.5s). `short/scenes.py` carried 9 hand-authored portrait (1080x1920) Manim scenes, same class names/PALETTE/MONO/helpers as the parent, same per-beat animation timing (every `self.play`/`self.wait` matching the parent beat-for-beat, since the audio is identical) — only the geometry redesigned for the narrow portrait column, with 3 real redesigns already called out in the file's own docstring: B04 (real filing vs. condition: parent's LEFT/RIGHT split → portrait TOP/BOTTOM stack), B05 (five-feed results table: parent's 4-column table → one mini-card per feed, stacked), B06 (before/after fix: parent's LEFT/RIGHT split → portrait TOP/BOTTOM stack) — each using the file's own `clear_of_hdivider()` helper (the rotated analogue of the parent's `clear_of_divider()`).
- 2026-08-31 — GATE A (`static_scene_check.py`) + GATE W (`wcag_margin_check.py`) run on all 9 portrait scenes before any render: 8/9 clean, 1 benign warning on B07 ("no shapes recorded — scene may be text-only," expected for a pure 3-line statement card, non-blocking).
- 2026-08-31 — First render pass (`./art run <reel>/short --height 1920`) hit **GATE B (`manim_layout_audit.py --portrait`) failures**, each a genuine layout bug caught before slotting, each fixed with real measurement (never eyeballed):
  - **B04 (real filing vs. condition):** the "checking for something that structurally can't appear" caption ran off the bottom of the frame (measured box y=[-4.09, -3.49], past the -3.4 safe floor) and the header top edge sat exactly on the +3.4 safe ceiling. Root cause: the vertical stack (header → top_block → bottom_block → caption) was sized for the parent's wider frame and didn't fit the portrait column's tighter ±3.4 y budget. Fixed by measuring every element's real `.get_top()`/`.get_bottom()` and tightening buffs (header edge-buff 0.6→0.75, top_block internal buff 0.18→0.12, header→top_block gap 0.4→0.3, bottom_block internal buff 0.3→0.2, top_block→bottom_block gap 1.0→0.4, bottom_block→caption gap 0.35→0.25) — re-measured result: header top y=3.25, caption bottom y=-3.22, both comfortably inside ±3.4.
  - **B05 (five-feed results table):** the header top edge measured y=3.45 (past the safe ceiling) and the closing caption's bottom edge measured y=-3.46 (past the safe floor) — the restacked one-column table (5 rows + header + caption) was taller than the portrait column's y-budget allowed at the original spacing. Fixed by widening the header's edge-buff (0.55→0.7) and tightening the table's row-to-row buff (0.28→0.2) and the header→table and table→caption gaps (0.4→0.35, 0.3→0.25) — re-measured result: header top y=3.3, caption bottom y=-3.19.
  - **B06 (before/after fix):** the AFTER code block's last lines ran past the safe floor (GATE B flagged the `${creator}\`;` line at y=[-3.44,-3.28]). Fixed by tightening the AFTER/BEFORE blocks' own internal line buff (0.07→0.05) and label-to-code buff (0.2→0.15), and widening both blocks' edge-buffs (BEFORE 0.65→0.8, AFTER 0.35→0.7) — re-measured AFTER block bottom at y=-3.3, safely inside the floor.
  - Re-ran GATE A/W/B after each fix; all 9 scenes clean before compiling.
- 2026-08-31 — Full portrait render (`./art run <reel>/short --height 1920`): 9/9 beats + 1 STILL endcard compiled real, master + review cut written at 1080x1920, 138.1s.
- 2026-08-31 — GATE V on the TRUE clean master (`final_frame_check.py <reel>/short --mp4 <path> --lenient`, run from inside the reel folder, not the watermarked `-slate.mp4` — the slate cut's own timecode watermark trips a false-positive edge-bleed on every beat, the true master does not): first pass **0 BLOCKER, 4 MAJOR/BLOCKER mix** — real defects, not the watermark artifact: B02's "0 MATCHES" stamp box and B05's CFTC-row highlight box both edge-bled past the safe x-extent (BLOCKER), and B07's statement card underfilled the safe area at 53% (MAJOR, min 55%).
- 2026-08-31 — Closed all 3 real defects with real Manim measurement:
  - **B02:** the "0 MATCHES" stamp's outer box (`stamp_text.width + 0.4` padding) measured ±2.09 x — past the safe x-extent — because the inner text was `fit()`-capped to the full `SAFE_W` (3.8) with no allowance for the box's own padding. Fixed by capping the stamp text to a narrower 3.2 width so the padded box lands at ±1.8.
  - **B05:** the CFTC-row highlight box was centered on `cftc_row.get_center()` — but table rows are left-aligned with varying widths, so the row's own center sits off from the table's true (symmetric) center, and a fixed-width box centered there overhung the left safe edge (measured -2.21). Fixed by centering the box's x on the table's own center (`table.get_x()`, matching how the zero-regression box below it was already built) instead of the row's own off-center center.
  - **B07:** re-measured the group's own bounding box at 53% coverage. Fixed by bumping font sizes (44/42/30, was 40/38/26) and inter-line buff (0.7, was 0.5) — re-measured group height 4.09→4.72, comfortably clearing the 55% floor while still bottoming out at y=±2.36, well inside the ±3.4 safe floor.
  - Cleared the stale `manim/` slots for the 3 changed beats, re-ran GATE A/W/B (all clean), and recompiled the full pipeline.
- 2026-08-31 — Final GATE V result on the true short master (`--lenient`): **0 BLOCKER, 2 MAJOR** — both remaining on the toolkit's own auto-generated silent END card (a PIL-drawn branded card produced by `shorts.py`'s `endcard_png()`, fixed layout, not one of this reel's 9 Manim scenes and not editable without touching `brutalist/`, out of scope per this task) — **0 BLOCKER, 0 MAJOR on all 9 authored Manim beats (B00-B08)**, same outcome as this fellow's sibling shorts.
- 2026-08-31 — Verified the final short master via `ffprobe`: **1080x1920, h264/aac, 138.07s**. Visually spot-checked `short/_qc/contact_sheet.png` — title card, exec-summary, "0 MATCHES" hook, the CFTC-branch code, the stacked real-filing-vs-condition redesign, the stacked five-feed table, the stacked before/after fix, and the takeaway statement all read clearly in portrait.
- Short master: `short/2026-08-30-the-check-that-never-once-fired-short.mp4` — 1080x1920, 138.07s, 10/10 real beats, 0 BLOCKER / 0 MAJOR on all 9 authored scenes (2 MAJOR remaining on the non-Manim endcard, flagged above).
- NOT AUTHORIZED — Publishing.

## Deliverables renamed (2026-08-31)

- `Mycroft_SaiPranaviJeedigunta_20260830b_16x9.mp4` (copy of the 16:9 master, 3840x2160, 133.55s)
- `Mycroft_SaiPranaviJeedigunta_20260830b_9x16.mp4` (copy of the 9:16 short master, 1080x1920, 138.07s)
- `b` suffix used to disambiguate from `Mycroft_SaiPranaviJeedigunta_20260830_16x9.mp4`, a different Mycroft video already published for the same date from a different fix.

## 2026-08-31 — Factual patch: B01's "last two weeks" wording removed

- Fellow flagged one line of B01's narration as factually wrong: it claimed the classifier sat "in the same regulatory pipeline from the last two weeks" — a timeframe claim not supported by the source material. Corrected wording (already reflected in `BEAT-SHEET.md`, `beat_sheet.json`, and `short/beat_sheet.json` before this patch): "...a classifier in the same regulatory pipeline I've been working on — a check specifically written to catch CFTC filings..." No other beat's narration touched.
- Audio-first pipeline, so the text fix required a real rebuild, not just an edit: regenerated `mp3/beat-B01.mp3` via `generate_audio_kokoro.py <reel> --only B01` (voice `af_bella`, from the corrected `narration_text`). `short/mp3/beat-B01.mp3` is a symlink to the parent's file, so it updated automatically — no separate short-side audio generation needed.
- New measured B01 duration: **18.74s** (was 18.98s, a −0.24s delta). `actual_duration_s` updated for B01 in both `beat_sheet.json` (auto-written by `generate_audio_kokoro.py`) and `short/beat_sheet.json` (updated by hand, since the script only touched the top-level sheet). New per-beat durations: 4.05/18.74/11.38/19.32/19.66/29.42/13.94/10.56/6.26s, total 133.33s.
- Checked `scenes.py`'s and `short/scenes.py`'s `B01_ExecSummary` — both hardcode a `self.wait(...)` derived from `measured narration − animation sum (2.8s)`, per this toolkit's audio-first convention. Both updated: `self.wait(16.18)` → `self.wait(15.94)` (2.8s animation + 15.94s wait = 18.74s, matching the new measured narration).
- Cleared the stale `manim/B01.mp4` slot (and `short/manim/B01.mp4`) so the retimed scene would actually re-render — `compile.py`/`./art final` only recompiles from existing slots, it doesn't re-render Manim itself. Re-ran `./art run <reel>` (top-level) and `./art run <reel>/short --height 1920`, each re-rendering only B01's Manim scene (GATE A/W clean) and recompiling both the slate and true clean master. `./art run`'s own internal GATE V step failed on both (18 BLOCKER on the 16:9 slate, 20 BLOCKER on the short slate) — this is the toolkit's known false positive: `final_frame_check.py` defaults to checking `*-slate.mp4` when no `--mp4` is given, and the slate cut's timecode watermark trips edge-bleed on every beat, not a real defect (consistent with the prior build's note above). Also re-ran `./art final <reel>` directly to confirm the clean 16:9 master compiles standalone.
- Re-ran GATE V by hand on the TRUE clean masters (`final_frame_check.py <reel_dir> --mp4 <absolute-path-to-clean-master> --lenient`, from inside each reel folder): **16:9 — 0 BLOCKER, 0 MAJOR** (18 frames). **9:16 — 0 BLOCKER, 2 MAJOR** (20 frames), both MAJOR on the `END_*` frames (toolkit's own auto-generated silent endcard, `underfill` at 31% vs the 55% floor — the same non-Manim, non-fixable-without-editing-`brutalist/` exception as the original build). **0 BLOCKER, 0 MAJOR on all 9 authored Manim beats (B00-B08)** in both cuts. Visually spot-checked both `_qc/contact_sheet.png`s — B01's corrected summary text reads clearly in both 16:9 and 9:16.
- New measured master durations: **16:9 — 133.32s** (was 133.55s, −0.23s). **9:16 — 137.83s** (was 138.07s, −0.24s) — both shifts consistent with B01's −0.24s audio delta.
- Re-copied both renamed deliverables, overwriting the corrected versions in place:
  - `Mycroft_SaiPranaviJeedigunta_20260830b_16x9.mp4` — now 3840x2160, 133.32s
  - `Mycroft_SaiPranaviJeedigunta_20260830b_9x16.mp4` — now 1080x1920, 137.83s
- `FACTCHECK.md` and `SOURCES.md` left untouched (out of scope for this patch — the correction was a scope/precision fix to the narration's own wording, not a new factual claim needing re-verification).
- NOT AUTHORIZED — Publishing.
