# Build log

- 2026-08-30 — Scoped video from Project 29's continued Layer 1 hardening pass: the "Mark email sent" node re-deriving its own copy of the high-priority rule instead of reading `High Priority Filter`'s output. Chosen over the still-open source-misclassification bug and the Google News link-unwrapping task (confirmed this week to be a bigger scrape-based project) because it has a clean, measured before/after (12 real rows right now that the old query would have wrongly flipped) and a sharp, general lesson.
- 2026-08-30 — `BEAT-SHEET.md`, `beat_sheet.json`, `FACTCHECK.md`, `SOURCES.md` authored and approved (Gate P). Both FACTCHECK open items resolved: B05's forward-looking "would have" framing approved as written (not "were" — `A7-VERIFICATION.md` explicitly does not prove the 12 rows were ever actually mis-marked in the live n8n workflow); the "12" count re-verified live against the DB at build time (21:04), identical ids to the original snapshot (112, 115, 153, 322, 326, 338, 347, 356, 364, 483, 555, 1008).
- 2026-08-30 — Voice: Bella (`af_bella`) — locked for this fellow's whole report series, unchanged from the 2 prior episodes.
- 2026-08-30 — Generated Kokoro audio for all 9 beats: B00 silent (`ffmpeg -f lavfi -i anullsrc=r=44100:cl=stereo -t 4`, measured 4.05s — NOT `audio_file: null`, per `compile.py`'s `build_master_audio()` all-beats-exist contract) and B01-B08 via `generate_audio_kokoro.py` (measured 18.02/13.44/16.68/23.52/21.55/7.97/10.61/5.62s). Updated `actual_duration_s` for every beat in `beat_sheet.json` from real `ffprobe` measurements — total runtime 121.46s.
- 2026-08-30 — Authored `scenes.py`: 9 Manim scenes (B00-B08), same PALETTE/MONO/`fit()`/`panel()`/`clear_of_divider()` helpers as this fellow's 2 sibling reels (`2026-07-26-recovering-the-silently-dropped-filings`, `2026-08-17-why-ai-generated-code-still-needs-a-human`) for house-style consistency. Every code/query string on screen (B02/B03/B04/B05/B06) is quoted verbatim from `A7-VERIFICATION.md` — nothing paraphrased. B03 (two conditions) and B06 (before/after SQL) are the reel's side-by-side beats; both use `clear_of_divider()` and were verified by measuring real Manim object bounds (not eyeballed) before rendering — both blocks clear the divider by >1.4 units on each side.
- 2026-08-30 — Added `SHOTLIST.md` and `PROMPTS.md` (GATE F paperwork set; this reel needs no pantry assets — all 9 beats are self-contained Manim).
- 2026-08-30 — GATE A/W static pre-flight caught 3 real issues before any render, all fixed with real Manim measurement (not guessed): (1) B02/B03/B04 each had "shapes never change — repeated animation" (only 1 non-text shape existing for the whole hold) — fixed by adding a second real shape (an underline Line created later than the first shape), same pattern as this fellow's sibling reels' B01/B08. (2) No other GATE A/W errors on first pass.
- 2026-08-30 — First `./art run` pass hit **GATE B (`manim_layout_audit.py`) failures**, each a genuine layout bug caught before rendering into the master, each fixed with real measurement:
  - B02: `node_sub` (the raw "UPDATE regulatory_feeds SET email_sent = TRUE" line, unfitted) rendered at width 6.71 against a 6.4-wide panel, overhanging both sides and crossing the panel's own rounded-rectangle stroke (TEXT_ON_CURVE). Fixed with `fit(node_sub, 5.7)`; also fixed a caption bottom edge at y=-3.61 (past the -3.4 safe floor) by tightening buffs 0.6/0.45/0.4 → 0.45/0.3/0.25.
  - B03: a fixed-length divider (`UP*2.6`/`DOWN*2.6`) extended down through the "these don't match" line below the two condition blocks (both near x=0, the divider's own x). Fixed by sizing the divider to the two blocks' own measured vertical span (`blocks_top`/`blocks_bottom`, +0.25 padding) instead of a guessed frame-spanning length.
  - B05: header top edge at y=3.45 (past the 3.4 safe ceiling) and the "12"-count group's right edge at x=6.87 (past the 6.3 safe wall, since query+count_group's combined span is itself off-center) — fixed the header buff (0.55→0.75) and the count_group buff (1.1→0.3); the `row_panel` (width 11.6, `next_to()`-only) had its right edge at x=7.63, past even the hard frame edge, because it inherited the off-center query+count span — fixed by trimming its width to 11.0 and explicitly `set_x(0)` (Y position only from `next_to()`, X independently centered).
  - Re-ran GATE A/W/B after each fix; all clean before the next full pipeline pass.
- 2026-08-30 — First full 4K render (`./art run`): 9/9 beats compiled real, master + review cut written (121.5s). GATE V on the review cut's own `-slate.mp4` showed 18 BLOCKER `edge-bleed` on every beat — confirmed as the same documented false-positive as this fellow's prior 2 reels (the review-only timecode watermark sits in the title-safe margin by design). Checked the TRUE clean master directly (`final_frame_check.py --mp4 <slug>.mp4`, not the slate): **0 BLOCKER, 10 MAJOR** (all `underfill`: B00 54%, B02 46%, B04 50-52%, B07 19%, B08 6% — all under the 55% floor).
- 2026-08-30 — Closed all 10 underfill MAJORs with real Manim-measured font/buff/width increases (same "measure real `.width`/`.height`, don't guess" method this fellow's sibling reels used): B00 buff 1.1→1.3; B02 panel widened 6.4→9.0 with proportionally larger condition/qmark/caption fonts and tightened buffs (kept within the safe y-floor, margin 0.3); B04 code font 18→20 and caption font 22→24 with wider buffs; B07 rewritten as bigger poster-style type (font_size 32/36/24 → 58/64/38, buff 0.55→1.5) rather than only widening gaps; B08 similarly (handle/tagline font_size 34/20 → 90/44, accent width 1.6→3.0, buff 0.35→2.3). Cleared stale `manim/`/`media/` slots for the 5 changed beats and re-ran the full pipeline.
- 2026-08-30 — Second full 4K render + GATE V on the true clean master: **0 BLOCKER, 0 MAJOR** on all 9 beats. Ran `./art final` per the documented workflow — output `2026-08-30-the-update-that-almost-lied-about-what-it-sent.mp4`, confirmed via `ffprobe`: **3840x2160, 24fps, h264/aac, 121.41s**. No stray `ffmpeg`/`manim`/`compile.py` processes found; file not corrupted. Re-ran GATE V on this freshly-written master: 0 BLOCKER, 0 MAJOR (unchanged).
- GATES CLOSED — plan, fact-check, narration, audio lock, previz. See `beat_sheet.json` → `metadata.gates` for the dated record.
- NOT AUTHORIZED — Publishing.

## 2026-08-30 — 9:16 Shorts derivative built (`short/`)

- 2026-08-30 — Ran `runtime/scripts/shorts.py` against this reel per THE SHORTS LAW: at 121.5s this reel is **under** the 180s cap, so the whole reel reformats 16:9→9:16 as-is — **0 beats dropped**, every beat's mp3 reused unchanged (symlinked from the parent's `mp3/`). Scaffolded `short/` with `beat_sheet.json` (`aspect_ratio: "9:16"`), a silent branded endcard (`media/END.png` + `mp3/beat-END.mp3`, 4.5s), and flagged all 9 beats as GENERATED (Manim) needing a real portrait scene in `short/scenes.py` — THE REFORMAT RULE never auto-crops generated graphics. First run used the script's `--handle`/`--next` defaults (`@nikbearbrown`, empty tease) — wrong for this fellow's channel; re-ran with `--handle "@HumanitariansAI"` and a substantive `--next` tease line (the full-breakdown teaser) to fix the endcard.
- 2026-08-30 — Authored `short/scenes.py`: 9 portrait (1080x1920) Manim scenes, same class names/PALETTE/MONO/helpers as the parent's `scenes.py`, same per-beat animation timing (every `self.play`/`self.wait` matches the parent beat-for-beat, since the audio is identical) — only the geometry redesigned for a ~3.8-4.0-unit-wide safe column instead of the parent's ~9-14-unit-wide frame. Applied the documented "portrait sync" fix (recompute `config.frame_width` from the real pixel aspect at import time — Manim CE's CLI sets pixel dims from `-r` but doesn't recompute `frame_width` to match) ported from this fellow's sibling shorts. Added `clear_of_hdivider()` (this file's rotated analogue of the parent's `clear_of_divider()`) for the 2 real redesigns:
  - **B03 (two conditions):** parent has High Priority Filter LEFT / Mark email sent RIGHT with a vertical divider. Portrait stacks TOP (High Priority Filter) / BOTTOM (Mark email sent) with a horizontal divider — same reading order the narration walks.
  - **B06 (before/after fix):** parent has BEFORE/AFTER SQL side by side. Portrait stacks BEFORE above / AFTER below (the narration's own order: "stop re-deriving the rule... read the exact ids...").
  - **B05 (live query proof)** also got a real redesign even though the parent wasn't strictly side-by-side: the parent's query+"12"-count sit beside each other with the example row below; portrait restacks all three strictly top-to-bottom (query, then "12", then the id-153 row card) for a single narrow column.
- 2026-08-30 — GATE A (`static_scene_check.py`) + GATE W (`wcag_margin_check.py --palette humanitarians`) run on all 9 new portrait scenes before any render. **Found a real toolkit limitation:** `static_scene_check.py` has no `--portrait` awareness (unlike `manim_layout_audit.py`, which does) and flags a false-positive "explicit coord outside the frame" on B05 (its render-free stub's own simulated arrange/next_to math diverges from real Manim for this scene's multi-line wrapped text, producing a fictitious y≈-4.3 that a real-manim probe confirmed does not exist — real content bottoms out at y≈-3.0, comfortably inside the -3.4 safe floor). Confirmed this is a stub artifact, not a real defect, by importing manim directly and measuring the actual scene's mobjects at the real portrait config (`pixel_width=1080, pixel_height=1920, frame_height=8.0` — same fix as the scenes.py itself) — documented here rather than silently suppressed.
- 2026-08-30 — GATE A also caught 2 real, fixable issues (not false positives): B04 lost its 2nd distinct shape-state when its floating "bypasses the score" tag was removed (see below) — fixed by adding a `caption1_underline` Line, same pattern as B02/B03. All other 8 scenes GATE A/W clean.
- 2026-08-30 — Rendered all 9 scenes + the endcard at native 1080x1920. Ran the REAL portrait-aware GATE B (`manim_layout_audit.py --portrait --curve-strict`) per class manually (since `run.sh`'s own GATE A/B/W trio is gated together by `ART_QC` and GATE A's stub false-positive on B05 would otherwise block the whole pipeline) — this is the authoritative pixel-true check for portrait geometry, and iterating against it (not eyeballing) caught 4 real layout bugs, each fixed and re-verified clean:
  - **B02:** the `node` panel was never repositioned off the frame origin (unlike the parent, which had this same bug before its own earlier fix) — the whole downstream condition/qmark/caption stack cascaded too far down and ran off the bottom of the portrait frame (measured off-frame at y=-4.22, past the -3.4 safe floor). Fixed by `node.next_to(header, DOWN, buff=0.35)` and re-tightening the downstream buffs; also bumped `node` height 1.3→1.5 since `node_label_group` (measured 1.40 tall) was poking past a 1.3-tall panel's own stroke.
  - **B03:** the "these don't match" mismatch text was centered exactly ON the horizontal divider's own y-coordinate — a genuine TEXT_ON_CURVE strike (both objects at literally the same y), not a false positive. Fixed by offsetting it 0.3 below the divider (still reads as "between" the two blocks; verified 0.20 clearance from the divider and 0.35 from the block below).
  - **B04:** the parent's separate "bypasses the score" floating tag (positioned `next_to(highlight, DOWN)`) landed directly on top of `code[7]`/`code[8]` ("return 'Medium'"/"return 'Low'") — code lines that continue physically below the highlighted lines in the same single-column `code` VGroup, a real text-on-text collision the portrait redesign introduced by not accounting for the trailing code. Fixed by **dropping the floating tag entirely** (the highlight box + caption text already carry the same meaning) and repositioning both captions `next_to(code, DOWN, ...)` — below the FULL code block, never just the highlighted subset — so they can never land on a later line.
  - **B05:** `row_content` (measured 1.995 tall) was poking past a 1.9-tall `row_panel`'s own stroke by ~0.05 on the top edge ("id 153" struck by the panel border) — same class of bug as B02's panel-overhang. Fixed by bumping `row_panel` height 1.9→2.2.
  - Re-ran GATE B after every fix; all 9 scenes clean (0 errors, 0 warnings) before compiling.
- 2026-08-30 — Compiled via `ART_QC=0 ./art run <reel>/short --height 1920` (skipping `run.sh`'s own bundled A/W/B trio, since it has no way to bypass just GATE A's documented false positive — the real portrait-aware GATE B check above was already run manually and independently, satisfying the same rigor). 10/10 slots filled (9 Manim beats + 1 STILL endcard), master + review cut written at 1080x1920, 125.96s.
- 2026-08-30 — GATE V on the TRUE clean master (`final_frame_check.py <reel>/short --mp4 <path> --lenient`, not the watermarked `-slate.mp4` — same documented false-positive re-confirmed: the slate cut's own timecode watermark trips edge-bleed on every beat, the true master does not): first pass **0 BLOCKER, 4 MAJOR** (`underfill` on B08 31% and the auto-generated END card 28%, both x2 for the 50%/85% samples).
- 2026-08-30 — Fixed B08's underfill with real Manim measurement (font_size 46/26 → 70/36, accent width 1.3→1.8, buff 0.5→1.4 — clears ~63% coverage while staying inside both safe-area walls, width 3.8/3.9 and height 4.36/6.8). Re-ran GATE A/W/B on B08 (clean), cleared its stale `manim/B08.mp4` slot, and used `shorts.py`'s own sanctioned `--next` override to give the endcard more substantive tease copy (the full-breakdown teaser, 4 lines instead of 2), raising its measured coverage. Re-ran the full pipeline.
- 2026-08-30 — Final GATE V result on the true short master: **0 BLOCKER, 2 MAJOR** — both remaining on the toolkit's own auto-generated silent END card (a PIL-drawn branded card produced by `shorts.py`'s `endcard_png()`, fixed layout, not one of this reel's 9 Manim scenes and not editable without touching `brutalist/`, out of scope per this task) — **0 BLOCKER, 0 MAJOR on all 9 authored Manim beats (B00-B08)**, same outcome as this fellow's sibling short.
- 2026-08-30 — Verified the final short master via `ffprobe`: **1080x1920, h264/aac, 125.96s**. Visually spot-checked `short/qc-sheet.png` (per-beat contact sheet) — every beat's key content (title, name/summary, the "Mark email sent" node, the stacked two-condition comparison with "these don't match", the highlighted `determineImpactLevel()` bypass, the stacked query+"12"+row-153, the stacked before/after fix with the `id = ANY($1::int[])` highlight, the takeaway, the brand outro) reads clearly in portrait.
- Short master: `short/2026-08-30-the-update-that-almost-lied-about-what-it-sent-short.mp4` — 1080x1920, 125.96s, 10/10 real beats, 0 BLOCKER / 0 MAJOR on all 9 authored scenes (2 MAJOR remaining on the non-Manim endcard, flagged above).
- NOT AUTHORIZED — Publishing.

## Deliverables renamed (2026-08-30)

- `Mycroft_SaiPranaviJeedigunta_20260830_16x9.mp4` (copy of the 16:9 master, 3840x2160, 121.41s)
- `Mycroft_SaiPranaviJeedigunta_20260830_9x16.mp4` (copy of the 9:16 short master, 1080x1920, 125.96s)
