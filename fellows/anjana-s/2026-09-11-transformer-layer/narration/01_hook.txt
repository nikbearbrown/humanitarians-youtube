A transformer model can have dozens of layers. But every layer does the same four things in the same order. Self-attention. Feed-forward. Layer norm. Residual connection. Here is what one token sees as it passes through one layer.