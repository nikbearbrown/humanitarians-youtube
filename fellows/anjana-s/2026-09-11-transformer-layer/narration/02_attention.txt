Step one. Self-attention. The token "revenue" needs to understand its role in the sentence. It creates three vectors from itself: a query, a key, and a value. The query asks "what should I pay attention to?" Every other token in the sentence also has a key. The query compares against every key and computes a score. High score means high relevance. "Revenue" scores high against "growth" and "moderate" because they directly affect its meaning. It scores low against "we" and "to" because they add no context. The scores become weights. The weighted sum of all values produces a new, context-aware representation of "revenue." The word has not changed. But now it knows what surrounds it.