# BUILD-LOG — Claude, Indexed.

## Decisions

- **6 points → 4 acts**, grouping the naturally-paired ones: I Indexing +
  Filtering (how a query actually runs), II Multitenancy + Scalability
  (how the system grows), III Drawbacks (its own act, one flaw per
  database), IV Use Cases (the closing decision framework).
- **No single source document** — content is general, stable,
  publicly-documented architecture across 5 real products, deliberately
  kept free of version numbers/benchmarks/pricing to avoid dating the
  video or making unverifiable claims. See `FACTCHECK.md`.
- **No Manim** (same `pangocairo` gap as every prior build) — absorbed
  into REMOTION.
- **Signed for Divyank from the start.** Unlike the `llm-as-a-judge` and
  `chapter-4` builds (originally signed "Nik Bear Brown," rebranded
  afterward), this beat sheet uses `@DivyankSingh` / "in for Divyank"
  throughout from first draft — no retrofit needed.
- **9:16 portrait coverage planned at authoring time**: `BinaryBranch`/
  `DivergentFates` assigned to one hero beat per act (A1-5, A2-3, A2-5,
  A3-4, A4-3) so the Short derivation needs no new Root.tsx work.
- **Location**: `humanitarians-youtube/fellows/divyank-s/2026-09-20-vector-databases/`
  — matching the established dated-fellow-report convention used for every
  prior build this session (no explicit location was given this time).
- **Two-deliverable plan**: one 16:9 master (this folder) and one 9:16
  Short (`short/`, via `shorts.py`), both at 4K (16:9: 3840x2160; 9:16:
  2160x3840, per the explicit spec this time).
