# PEDAGOGY — Claude, Judged. (deep-explainer, claude-liam)

Fresh technical build, no single source document (see `FACTCHECK.md`'s
method note). Thesis: a real LLM-as-a-judge evaluation system is eight
components that most harnesses reinvent independently — test case
structure, judge prompts, output parsing, bias mitigation, metric
abstraction, a batch evaluation runner, aggregation, and CI/CD integration
— covered here as four acts of two points each.

## Spine check

- B00 cold open, IN-FOR-BEAR LAW narration ✓
- 4 acts × (1 `FluencySegmentCard` + 6 content beats) = 28 body beats ✓
- VERDICT (`ClaudeVerdictArtifact`) → HANDOFF (`ClaudeComposerAsk`, "Your
  turn.") → title-restate OUTRO — the `your-turn` three-beat standard ✓
- SHOW-DON'T-TELL: every beat's `shot.show` authored before narration
  finalized ✓

## Beat-mix contract (deep-explainer's own quota)

Programmatically checked (not eyeballed): 28 body beats — **VOX 6/28 =
21.4%** (inside the 20-25% target, well inside the 15-30% no-warning
band), REMOTION 18/28 = 64.3% (absorbing the documented MANIM ~25-40%
share, since no Manim on this machine), CARD 4/28 (the four act titles).
No two consecutive body beats share a lane except where a VOX pair opens
back-to-back acts (never happens here — each act's two VOX beats are
separated by REMOTION beats).

## Narration budget

Body beats run 20 (shortest) to 34 words per programmatic check — narrower
than the 25-45 target on a few beats, none over it; act cards (5 words
each) are exempt one-liners by design, not body beats.

## VOX honesty

All 6 archival stills are used as **illustrative metaphor** for a modern
software pattern with no historical referent — every beat's `shot.prompt`
and narration frames them that way (e.g. "illustrative... not a literal
depiction" in the `SOURCES.md` sidecars), never implying the photos show
LLM evaluation itself.

## Time-limit requirement

The human's standing instruction: the video must not run shorter than the
skill's documented floor (deep-explainer = 5-10 minutes → 300s floor).
Planning estimate: 493s (8.2 min) — well over the floor even allowing for
Kokoro's typically-faster-than-estimated real pace (observed ~15-20%
faster on the prior deep-explainer build this session).

## Status

**VERDICT: PASS** — signed off by the human (Divyank Singh,
singh.divya@northeastern.edu) in chat, 2026-07-25, on review of this
PEDAGOGY.md, FACTCHECK.md, and the beat sheet's narration.
