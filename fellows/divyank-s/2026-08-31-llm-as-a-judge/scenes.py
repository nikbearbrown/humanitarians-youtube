"""
Manim scenes for claude-liam-llm-as-a-judge ("Claude, Judged.")

No Manim beats in this reel — this machine has no pangocairo/pkg-config
(see BUILD-LOG.md). Every beat is Remotion (ClaudeComposerAsk,
FluencySegmentCard, ClaudeScienceLayerStack, ClaudeScienceChipGrid,
BinaryBranch, DivergentFates, ClaudeVerdictArtifact, ClaudeTitleOutro) or a
VOX still (real archival photographs in media/, animated by compile.py's
Ken Burns). This file exists only to satisfy run.sh's per-reel scenes.py
guard (it refuses to render the shared animated_graphics.py fixture scenes
into a reel that isn't vox-electoral-college).
"""
