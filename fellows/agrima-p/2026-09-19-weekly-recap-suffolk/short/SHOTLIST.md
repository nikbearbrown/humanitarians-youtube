# SHOTLIST — weekly-recap-suffolk short (9:16, true 4K vertical)

Derived from the parent 16:9 reel via shorts.py. Parent is 2:12.5, well
under the 3:00 Shorts cap, so shorts.py's auto-plan dropped NOTHING —
every parent beat carries over 1:1, plus a silent branded endcard.

## Composer / outro beats — Claude-skin Remotion, re-rendered portrait (916)

B00 · ClaudeComposerAsk916 — cold open, self-intro folded in ("Hi, I'm Agrima.")
B02 · ClaudeComposerAsk916 — the ask: write weekly_recap_v1.py
B03 · ClaudeCodeBeat916    — the REAL v1 code + its real terminal output
B05 · ClaudeComposerAsk916 — the change: revise v1 -> v2 (split DONE/NEXT)
B06 · ClaudeCodeBeat916    — the REAL v2 code
B09 · ClaudeComposerAsk916 — HANDOFF LAW, viewer's own recap prompt
B10 · ClaudeTitleOutro916  — restates the series title

## Manim GRAPHIC beats — short/scenes.py (portrait relayout)

B01 · B01_NotAHighlightReel — typographic card, generic framing (single column)
B04 · B04_FlatWeek — three same-weight cards, STACKED (article / four-videos /
        Suffolk) instead of the parent's side-by-side row — a 9:16 canvas
        has no room for three cards abreast
B07 · B07_SplitWeek — same three cards, regrouped: DONE THIS WEEK (article +
        four-videos side by side, smaller) vs STARTING NEXT WEEK (Suffolk
        card, full width), stacked vertically with a divider line
B08 · B08_TheLesson — typographic closing beat

## END · silent branded card

Generated by shorts.py (endcard_png), ~4.5s, handle @HumanitariansAI. At
true 4K (--height 3840, 2160x3840 output) shorts.py's own find_serif() has
a known gap on this Windows install (documented earlier this session on
every prior short built here) — media/END.png is regenerated manually with
Georgia (C:\Windows\Fonts\georgia.ttf) at 2160x3840 before the final
compile, matching the fix applied on every other short this session.

## Notes

- No stock footage or screen recording used — every Manim visual is
  generated fresh in scenes.py, matching the parent's own visuals: a mock
  article header for "No Face, No Problem," a 2x2 four-video grid, and a
  Suffolk podium/presentation-slide card.
- B04/B07's card reveal carries forward a fix made on the PARENT after its
  first 16:9 render: growing a card's box and fading in its (already
  full-size) text at the same time briefly let the text overhang the
  still-narrow box mid-grow, worse once a short beat gets slowed several
  times over to match its narration. `_reveal_card` now grows the box to
  full width FIRST, then fades the text in — applied here from the start.
- Audio-first: all 12 mp3s (11 parent beats + none new) are the SAME
  Kokoro (af_bella) files as the parent, copied by shorts.py verbatim
  since this is a full-parity reformat — no beats were dropped or
  rewritten, so no new narration was generated.
- Rendered via this toolkit's own run.sh/compile.py/static_scene_check.py/
  manim_layout_audit.py/final_frame_check.py pipeline, same as the parent
  and every other reel this session — the cli-explainer skill's documented
  vox_run.sh/vox_compile.py/type_check.py do not exist in this install.
- True 4K vertical requires `--height 3840` passed explicitly to run.sh.
