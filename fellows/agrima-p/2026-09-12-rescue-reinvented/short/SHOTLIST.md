# SHOTLIST — rescue-reinvented/short (9:16 Shorts cut)
# Derived from the parent 16:9 reel via shorts.py. Parent is 3:56.8, over
# the 3:00 Shorts cap, so shorts.py auto-dropped 2 beats (longest middle
# beats first, hook/hero/outro protected): B03 (staff/volunteer time win)
# and B05 (honest numbers). The rewritten outro (B08) names what got cut
# and points to the long.

## Composer/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render) — plain ask hook
B07 · ClaudeComposerAsk916 (Remotion, portrait re-render) — HANDOFF LAW
B08 · ClaudeTitleOutro916 (Remotion, portrait re-render) — outro NARRATION
      rewritten (only audio regenerated for this Short) to name the cut
      material and send viewers to the long; title/handle unchanged

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B00B · B00B_AgrimaIntro — presenter card ("Hi, I'm Agrima." + lead-in),
       single-column portrait layout
B01   · B01_TheBackdrop — 2x2 grid of strain-point cards (unchanged content,
        narrower type)
B02   · B02_LostPetMatch — lost/found photo-match graphic, stacked
        vertically instead of side-by-side
B04   · B04_CoordinationHub — central card + three fanned tags, adapted for
        a narrow portrait column
B06   · B06_NotSmarterAI — reframe card (vertical stack, unchanged)

## Dropped from this Short (content lives only in the parent long)

B03 · B03_NotesToContent — one input card fanning into three output cards
      (adoption post, social caption, donor update) — win #2, staff and
      volunteer time
B05 · B05_HonestNumbers — the two-stat contrast (4.2M adopted in 2025 vs.
      shelter capacity still stretched thin) — the honest-context beat;
      the rewritten B08 outro explicitly teases this ("the honest numbers
      behind this progress... where the gaps still are") to send viewers
      to the long instead of resolving it here

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle set to @HumanitariansAI via --handle at plan time; if
      compiling at true 4K (--height 3840), regenerate END.png manually
      with Georgia (C:\Windows\Fonts\georgia.ttf) at 2160x3840 — shorts.py's
      find_serif() has no Windows path and silently falls back to PIL's
      tiny bitmap font otherwise (fix used on every prior short this
      session).

## Notes

- No pantry / archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly, with all text/cards guarded
  to the 1.95-half-width portrait safe band (manim_layout_audit.py
  --portrait: safe_w=1.95, safe_h=3.4) and generous buff values (>=0.7) on
  any to_edge() call.
- Only B08's narration was regenerated (Kokoro) — every other beat reuses
  the parent's mp3 unchanged, per THE SHORTS LAW.
- @HumanitariansAI used throughout, matching branding precedent.
