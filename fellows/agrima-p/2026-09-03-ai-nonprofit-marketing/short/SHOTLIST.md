# SHOTLIST — ai-nonprofit-marketing/short (9:16 Shorts cut)
# Derived from the parent 16:9 reel via shorts.py. Parent is 3:59.2, over
# the 3:00 Shorts cap, so shorts.py auto-dropped 4 beats (longest middle
# beats first, hook/hero/outro protected): B02, B04, B06, B08. The
# rewritten outro (B11) names what got cut and points to the long.

## Composer/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render) — plain ask hook
B10 · ClaudeComposerAsk916 (Remotion, portrait re-render) — HANDOFF LAW
B11 · ClaudeTitleOutro916 (Remotion, portrait re-render) — outro NARRATION
      rewritten (only audio regenerated for this Short) to name the cut
      material and send viewers to the long; title/handle unchanged

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B00B · B00B_AgrimaIntro — presenter card ("Hi, I'm Agrima." + lead-in),
       single-column portrait layout
B01   · B01_OneTeamManyHats — one-person card with four role tags, kept as
        a diamond/vertical fan instead of a wide horizontal spread
B03   · B03_AdoptionStats — 50%+ / ~30% stat lines, stacked (unchanged
        content, narrower type)
B05   · B05_EmailPersonalization — generic vs personalized email, stacked
        vertically instead of side-by-side
B07   · B07_HonestLimit — three-line contrast (vertical stack, unchanged)
B09   · B09_LoonClose — closing typographic beat + Loon Project caption
        (vertical stack, unchanged)

## Dropped from this Short (content lives only in the parent long)

B02 · B02_LoonBudget — full-team vs one-person budget comparison
B04 · B04_UnglamorousTasks — the four automated task types, 2x2 grid
B06 · B06_DonationFormLift — $115 vs $161 donation-form bar comparison
B08 · B08_NotTopDown — "not big tech, top-down" reframe

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle set to @HumanitariansAI via --handle at plan time; if
      compiling at true 4K (--height 3840), regenerate END.png manually
      with Georgia (C:\Windows\Fonts\georgia.ttf) at 2160x3840 — shorts.py's
      find_serif() has no Windows path and silently falls back to PIL's
      tiny bitmap font otherwise (fix already used on two prior shorts
      this session).

## Notes

- No pantry / archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly, with all text/cards guarded
  to the 1.95-half-width portrait safe band (manim_layout_audit.py
  --portrait: safe_w=1.95, safe_h=3.4) — generous buff values (>=0.7) on
  any to_edge() call, learned from two GATE B near-miss fixes on the
  parent 16:9 build this session.
- Only B11's narration was regenerated (Kokoro) — every other beat reuses
  the parent's mp3 unchanged, per THE SHORTS LAW.
- Measured total: ~174.8s (2:54.8) estimated pre-audio-regen, comfortably
  under the 3:00 cap.
- @HumanitariansAI used throughout, matching branding precedent.
