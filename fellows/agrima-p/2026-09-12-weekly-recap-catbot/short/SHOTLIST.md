# SHOTLIST — weekly-recap-catbot/short (9:16 full-parity reformat)

Derived from the parent 16:9 reel via shorts.py. Parent is 2:10.3, well
under the 3:00 Shorts cap, so shorts.py's auto-plan dropped NOTHING — every
one of the parent's 11 beats is present here, portrait-relaid-out.

## Composer/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render) — cold open
B02 · ClaudeComposerAsk916 — the ask
B03 · ClaudeCodeBeat916 — the real v1 code
B05 · ClaudeComposerAsk916 — the change
B06 · ClaudeCodeBeat916 — the real v2 code
B09 · ClaudeComposerAsk916 — HANDOFF LAW
B10 · ClaudeTitleOutro916 — outro (unchanged narration/title, full parity)

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B01 · B01_NotAHighlightReel — typographic card, unchanged content
B04 · B04_FlatWeek — three same-weight cards STACKED vertically (article
      header / video-production / cat-bot), scaled down from a comfortable
      base size so all three plus title+footer fit the portrait safe band
B07 · B07_SplitWeek — same three cards, regrouped DONE/NEXT, stacked
      vertically with a horizontal divider (adapted from the parent's
      side-by-side layout for the narrow column)
B08 · B08_TheLesson — typographic closing beat, unchanged content

## Dropped from this Short

None — full parity, per THE SHORTS LAW (reels <=3:00 reformat as-is).

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle @HumanitariansAI; if compiling at true 4K (--height 3840),
      regenerate END.png manually with Georgia
      (C:\Windows\Fonts\georgia.ttf) at 2160x3840 — shorts.py's find_serif()
      has no Windows path and silently falls back to PIL's tiny bitmap font
      otherwise (fix used on every prior short this session).

## Notes

- No pantry/archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py).
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly, with all text/cards guarded
  to the 1.95-half-width portrait safe band (manim_layout_audit.py
  --portrait: safe_w=1.95, safe_h=3.4).
- All 11 beats' narration/audio are byte-identical to the parent (full
  parity) — no new Kokoro generation needed beyond re-measuring durations.
- @HumanitariansAI used throughout, matching branding precedent.
