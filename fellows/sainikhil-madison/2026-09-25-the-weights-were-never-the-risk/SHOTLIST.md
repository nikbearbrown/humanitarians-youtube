# SHOTLIST — The Weights Were Never the Risk

Typed work order. Ten beats, two deliverables (16:9 and the full-length 9:16),
**no open slots**: every beat is a registered Remotion composition rendered from
props in `beat_sheet.json`, which is itself generated by `build_beats.py`.
Nothing is waiting on generation, purchase, a pantry still or an approval. There
is no user media in this reel at all.

**Lane key** — `remotion` = rendered by `remotion_scenes.py`.

| Beat | Act | Lane | Asset | Motion | In 9:16? |
|---|---|---|---|---|---|
| B00 | ASK | remotion | `ClaudeComposerAsk` | illustrate | yes → `ClaudeComposerAsk916` |
| B01 | THE SWAP | remotion | `ClaudeScienceChipGrid` | illustrate | yes → `ClaudeScienceChipGrid916` |
| B02 | THE RISK | remotion | `BinaryBranch` | illustrate | yes → `BinaryBranch916` |
| B03 | THE PIXELS | remotion | `TypesetMath` | illustrate | yes → `TypesetMath916` |
| B04 | THE PROOF | remotion | `ExecutedData` | illustrate | yes → `ExecutedData916` |
| B05 | THE RULER | remotion | `DivergentFates` | illustrate | yes → `DivergentFates916` |
| B06 | WHAT IT IS FOR | remotion | `ClaudeScienceChipGrid` | illustrate | yes → `ClaudeScienceChipGrid916` |
| B07 | VERDICT | remotion | `ClaudeVerdictArtifact` | illustrate | yes → `ClaudeVerdictArtifact916` |
| B08 | HANDOFF | remotion | `ClaudeComposerAsk` | illustrate | yes → `ClaudeComposerAsk916` |
| B09 | OUTRO | remotion | `LogoOutro` | illustrate | yes → `LogoOutro916` |

All ten `916` siblings were confirmed as real `<Composition id=…>` registrations
in `Root.tsx`. Because there is no user media, `shorts.py --vertical` rewires
every beat with no hand-composed plate.

---

## B03 — the equation beat

Outlined SVG from `runtime/scripts/typeset_math.py` (matplotlib mathtext, STIX,
`svg.fonttype=path`), embedded as base64 data URIs. Reveal times are cued by
`build_beats.py` to the word that introduces each row in the measured mp3.

| Row | Expression | Reveal | Aspect |
|---|---|---|---|
| 1 | `r = \min\left(\left\lfloor \dfrac{w}{640} \right\rfloor,\ \left\lfloor \dfrac{h}{640} \right\rfloor\right)` | 2.02s | 5.63 |
| 2 | `f = \max\,\{\, d \in \{1, 2, 4, 8\} : d \leq r \,\}` | 4.63s | 13.79 |
| 3 | `r = \min(\lfloor 8.7 \rfloor,\ \lfloor 5.8 \rfloor) = 5 \ \Rightarrow\ f = 4` | 10.44s | 14.11 |

## B04 — the executed-data beat

| Row | Value | Reveal |
|---|---|---|
| boxes Python found | 32 | 4.62s |
| found again by Rust | 32 | 7.18s |
| worst confidence gap | 0.0151 | 9.75s |
| change in AP@0.5 | -0.0011 | 14.89s |

Values are copied from `evidence/parity_rust.out`, `evaluate_rust.out` and
`evaluate_python.out` by `build_beats.py`; nothing is typed by hand.
