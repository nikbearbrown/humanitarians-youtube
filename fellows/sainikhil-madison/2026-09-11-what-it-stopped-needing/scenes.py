"""
Manim scenes for claude-sai-what-it-stopped-needing.

INTENTIONALLY EMPTY — this is an all-Remotion ai-explainer reel. Every graphic
beat is a registered Remotion composition rendered by remotion_scenes.py, and
the single evidence beat (B02) is a still composed by make_plates.py. There are
no Manim beats to declare.

This file exists only to satisfy the guard in run.sh, which refuses any reel
folder without a scenes.py. Do NOT add a scene declaration here, and do not
describe one in prose either: the GATE F scanner matches raw text, not parsed
code, so a declaration written inside a comment or a docstring is enough to make
the build think a Manim beat is pending and start asking for a shotlist.
"""
