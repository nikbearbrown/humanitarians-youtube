"""
Manim scenes for claude-sai-model-to-the-bill.

INTENTIONALLY EMPTY — this is an all-Remotion ai-explainer reel. Every beat is a
Remotion pattern rendered by runtime/scripts/remotion_scenes.py, so there is
nothing here to declare.

This file exists only to satisfy run.sh's guard, which refuses any reel folder
without a scenes.py. Keep it free of Manim scene declarations entirely: the gate
scanner reads TEXT, not code, so writing one inside a comment or a docstring is
enough to make GATE F think a Manim beat is pending and demand a FACTCHECK /
SHOTLIST / PROMPTS set that this reel does not have.
"""
