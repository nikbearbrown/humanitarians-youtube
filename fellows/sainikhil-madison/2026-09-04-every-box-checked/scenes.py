"""
Manim scenes for claude-sai-every-box-checked. INTENTIONALLY EMPTY — this is an
all-Remotion ai-explainer reel. Seven beats are Remotion patterns rendered by
remotion_scenes.py and one (B02) is a composed still plate built by
make_plates.py; there are no Manim beats to declare.

This file exists only to satisfy run.sh's guard, which refuses any reel without
a scenes.py. Keep it free of scene-class declarations entirely — run.sh's GATE F
scanner reads TEXT, not code, so even one inside a docstring or a comment makes
it think a Manim beat is pending and demands FACTCHECK/SHOTLIST/PROMPTS.
"""
