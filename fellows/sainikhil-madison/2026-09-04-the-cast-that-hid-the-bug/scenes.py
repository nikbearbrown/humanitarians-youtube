"""
Manim scenes for claude-sai-the-cast-that-hid-the-bug.

INTENTIONALLY EMPTY — this is an all-Remotion ai-explainer reel. Every beat is
a registered Remotion composition rendered by remotion_scenes.py, so there are
no Manim beats to declare here. This file exists only to satisfy run.sh's
guard, which refuses any reel folder that has no scenes.py at all.

Do not add scene declarations to this file: run.sh's gate scanner reads it as
text, not as code, so even a commented-out declaration would make GATE F think
a Manim beat is pending and demand a shotlist for it.
"""
