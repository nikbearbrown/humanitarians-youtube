"""
Manim scenes for claude-sai-every-image-earns-its-place.
INTENTIONALLY EMPTY — this is an all-Remotion weekly-progress reel. Seven beats
are Remotion patterns rendered by remotion_scenes.py and two are composed stills
built by make_plates.py; there are no Manim beats to declare. This file exists
only to satisfy the run.sh guard, which refuses any reel folder with no
scenes.py at all.

Do not add a scene declaration here, not even inside this docstring: the gate
scanner reads raw text rather than parsing Python, so the mere string would make
GATE F believe a Manim beat is pending and start asking for a factcheck and a
shotlist that this reel has no use for.
"""
