# Visual QC report — How Do You Test a Finance Agent? (this week)

Method: sampled frames + read PNGs (VISUAL QC LAW) + full contact sheet. 14/14 beats render as
real visuals — zero slates. Zero BLOCKER, zero MAJOR defects.

Checked: B04 / B06 real `evaluation.py` code fit the card with no horizontal clipping (B06's
four mutation lines are the longest; all inside the card). B05 chip grid shows all four defects.
B07 / B09 windows (behaviors+baseline, scorecard 7/7 + 24 tests) read cleanly. BVDT five verdict
lines fit. 100% Remotion by design; deep-explainer quota warning expected/accepted.
