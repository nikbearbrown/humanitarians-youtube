# Build log

- 2026-07-27 — Authored a render-ready weekly-report plan for fellow Adwait Changan
  from the Mycroft Finance Investigator project (three weeks of work: dataset → engine → agent).
- 2026-07-27 — Applied HAI policy: `@HumanitariansAI`, Pragmatist register. Fellow-selected
  voice **`am_onyx`** ("Onyx, in for Humanitarians AI") — persistent across this fellow's series.
- 2026-07-27 — Chose only Remotion patterns that exist in the pared-down `brutalist.art`
  toolkit (ClaudeComposerAsk, ClaudeCodeBeat, ClaudeVerdictArtifact, ClaudeTitleOutro,
  ClaudeWindow, ClaudeScience* structural illustrations). Number/variance beats that would
  ideally be Manim are marked to render as honest slates until Manim is installed or a
  Remotion illustration is authored (Manim intentionally NOT installed — minimal toolkit).
- 2026-07-27 — Gate P signed PASS at PREVIZ scope (fellow, chat). Generated 13 am_onyx MP3s
  (~192s total = master clock). Rendered all 13 Remotion beats at 4K → compiled review cut
  `...-slate.mp4` (191.7s, 13/13, zero slates).
- 2026-07-27 — VISUAL QC pass (read frames): fixed 2 defects → (1) B02 ChipGrid / B03 LayerStack /
  B08 SourceFlow only partially revealed because the `ClaudeScience*` compositions are registered
  at 900 frames (30s) in the toolkit's Root.tsx and their staggered reveal was truncated to our
  shorter beats; shortened those three comps to 360 frames (12s) so the animation completes and
  then freeze-holds to each beat's audio length. (2) Removed a meta-comment burned into the B05
  on-screen code. Re-rendered B02/B03/B05/B08 and recompiled. QC now clean across all 13 beats.
  NOTE: the Root.tsx edit is a local brutalist.art toolkit adaptation — NOT part of this repo.
- 2026-07-27 — REVISION pass (fellow fact-confirmation): metrics confirmed against the stored
  sample run. Applied fellow's changes → "43 data rows across six synthetic datasets" (not
  "records"); investigator = "local, evidence-driven agent — no external model in the loop"
  (B00/B07/B08); B05 code swapped for the real `finance.py:162 ebitda_variance()`; B06 variance
  shown as −$120,000 (actual − budget) to match the code's sign; added the synthetic/DRAFT +
  named-human-reviewer disclosure to the verdict (spoken + on-screen line). Regenerated audio
  for the 5 changed-narration beats, re-rendered 7 changed media beats, recompiled. FACTCHECK
  CLEARED; PEDAGOGY re-signed at FINAL scope. Wrote FLOW-REVIEW.md (Codex/reviewer handoff).
- OPEN GATE — Human plan approval (act map + lane histogram).
- OPEN GATE — Human fact-check approval (`FACTCHECK.md`; verify the reported metrics).
- OPEN GATE — Human narration approval before audio (`PEDAGOGY.md`).
- BLOCKED BY DESIGN — audio lock not started; `SHOPPING.md` waits for measured audio.
- NOT AUTHORIZED — Publishing.
