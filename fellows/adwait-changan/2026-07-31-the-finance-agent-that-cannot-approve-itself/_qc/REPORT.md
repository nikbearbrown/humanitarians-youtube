# Visual QC report — The Finance Agent That Cannot Approve Itself (Week 31)

Method: sampled frames with ffmpeg and read the PNGs (VISUAL QC LAW), plus the full contact
sheet (`qc-sheet.png`). 15/15 beats render as real visuals — **zero slates**.

## Result
Zero BLOCKER, zero MAJOR defects. First render pass clean (the `ClaudeScience*` composition
duration fix from Week 30 is already in the local toolkit, so the four-rule LayerStack (B06)
reveals fully within its beat).

Checked specifically:
- **B05 / B07 (real `review.py` code)** — both fit the code card with no horizontal clipping;
  the longest line (`APPROVE requires at least one evidence-backed causal explanation`) sits
  inside the card. PYTHON badge correct (the files are Python).
- **B02 / B08 / B10 (ClaudeWindow artifacts)** — verified run, schema fields, and the CLI
  self-approval rejection all read cleanly and fit.
- **BVDT** — five verdict lines incl. the OPEN / no-external-LLM / nothing-fabricated
  disclosures, within the card.

Note: this reel is 100% Remotion by design; the deep-explainer VOX/Manim quota warning from
`compile.py` is expected and accepted.
