# PROMPTS — "Testing AI Video Editors, and a Setup Guide for New Fellows"

Component briefs. Nothing was generated by an image or video model; B03's frames
are extracted from the HyperFrames test's own render.

## The library-first gate

| Need searched | Result | Used |
|---|---|---|
| "four tool comparison cards" | `HaiProgressSignupChain` (week-02) | **yes, for B06** — the account chain it was built for, with Canva replacing Adobe CC and the docStatus IN PROGRESS |
| "problems list stamped fixed cards" | `HaiProgressOverturned` (week-03) | **yes, for B04** — its was → now rows fit "what went wrong → what it took" |
| "dependency map onboarding steps accounts" | `HaiProgressRoadmap` | no — a timeline, not a chain |
| "video frame screenshot showcase own footage" | `SeisShowcase` | no — a photo wall for a different brand |
| "two bottlenecks pipeline time" | `BrandB01PipelineGap` | no — another channel's grammar |
| "verdict good at not good at" | `ClaudeVerdictArtifact` | no — a centred document card; underfills Gate V |

Reused: `ClaudeComposerAsk` (B00, with the new opt-in `disclosure` line),
`HaiProgressOverturned` (B04), `HaiProgressSignupChain` (B06), `HaiTitleOutro` (B07).

## New components

**B01 `HaiProgressTwoFronts`** — a clock face with a sweeping hand (the shared
cause), then two cards, EDITING and ONBOARDING: TODAY on top, THE GOAL beneath.
When the goals arrive the today lines strike through in spark and the clock hand
slows.

**B02 `HaiToolField`** — four cards landing on their spoken names, each carrying
an animated drawing of its idea of a video (code lines typing; a `<video>` tag in
a browser window; timeline tracks under a moving playhead; a grading colour
wheel). No logos. HyperFrames receives a TESTED HANDS-ON stamp as the beat ends;
Resolve's Studio caveat turns spark on "paid Studio version".

**B03 `HaiFootageShowcase`** — an INPUT → ASK strip over a monitor playing four
real output frames (glass intro, captioned cold open, "Getting Signed In" with
its lit caption, "Text In. Finished Track Out."), cross-faded on cues with a slow
push. Measured results count in beside it: 4:05, 94 caption lines, 0 overlaps.

**B05 `HaiVerdictSplit`** — GOOD AT (ink ticks) and NOT YET (spark rings)
columns, then a dashed NOT TESTED YET strip.

Every new component has a native `<Pattern>916` portrait layout inside the
y 230–1440 safe band.
