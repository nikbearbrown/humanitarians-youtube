# SHOTLIST — `yatra-nobody-wrote-this` ("Nobody Wrote This.")

Typed work order. Every slot is machine-rendered; **nothing is owed by a human**.
There is no pantry item, no still to source, no screen recording to capture, and
no HOLD anywhere in this reel.

Channel `claude-yatra` · handle `@Yatra` · voice Kokoro `af_bella` ·
palette `claude` (fidelity — never retint) · 16:9 master, 9:16 derived.

| Beat | Len | Act | Scene (16:9 / 9:16) | The shot |
|---|---|---|---|---|
| B00 | 12.59s | COLD OPEN | `ClaudeComposerAsk` / `…916` | Cream composer, greeting `Merhaba, Yatra`. The ask types in, send arms terracotta, three result lines land — the cold open is ANSWERED. |
| B01 | 10.45s | BLUF | `LnkBluf` / `LnkBluf916` | Serif statement sets; the thesis phrase lands terracotta. "Not lightly assisted." types in, is struck through, and "Written for them." replaces it. |
| B02 | 12.20s | FRAMEWORK | `LnkFrame` / `…916` | Three EMPTY bins draw top to bottom. The third bin's outline reddens on "watch which bin fills". No figure anywhere. |
| B03 | 12.91s | THE FIGURE | `LnkStat` / `…916` | `41%` rises oversized in terracotta serif; label, then the ordinal rank chip, then the citation, held. |
| B04 | 7.36s | ASK | `ClaudeComposerAsk` / `…916` | Composer returns with the real generation prompt for the next beat's chart. `output: []` — the result IS B05. |
| B05 | 14.51s | RESULT | `LnkLadder` / `…916` | Five bars stagger in, LinkedIn terracotta. Ranges print verbatim. Dashed reference drops at the cross-platform rate on "one in four". |
| B06 | 10.77s | DISPROPORTION | `LnkDisproportion` / `…916` | Track one fills to a third, ink. Track two blows past it to nearly 62%, terracotta. Dashed carry-down marks where proportional would have stopped. |
| B07 | 11.07s | THE TELL | `LnkAllOrNothing` / `…916` | B02's bins return and fill: the assisted middle a 4.3% terracotta sliver, "generated whole" at 41% ink, the remainder a dashed EMPTY band with no figure. |
| B08 | 12.39s | CONTRADICTION | `LnkContradiction` / `…916` | "Ships Enhance Post" and "Penalises AI content" drive at each other; the drives meet and stall in a terracotta block labelled "same platform". |
| B09 | 10.90s | FALSIFIABILITY | `LnkFalsify` / `…916` | Three stress-tests stagger in with their reasons; closer lands in serif. |
| B10 | 12.35s | STAKES | `LnkPressure` / `…916` | Two blocks: one tagged `INTERPRETATION`, one tagged `IN FORCE` with its citation. A date axis draws beneath and the Aug 2, 2026 marker lands terracotta. |
| B11 | 17.28s | VERDICT | `ClaudeVerdictArtifact` / `…916` | Artifact page; four cited lines land one per spoken clause. |
| B12 | 18.47s | YOUR TURN | `ClaudeComposerAsk` / `…916` | Greeting `Your turn.`; the prompt types itself as it is read aloud verbatim; three-item rubric stacks in. |
| B13 | 5.18s | OUTRO | `ClaudeTitleOutro` / `…916` | "Nobody Wrote This." poster serif, terracotta period, `@Yatra` beneath, no subline. |

**Total 168.43s — 2 min 48 s.**

## Standing rules this shotlist is built against

- **ILLUSTRATE LAW.** The Claude UI appears at B00, B04, B11, B12, B13 only.
  Every other beat illustrates. No two consecutive beats share a visual scheme —
  the B02→B07 callback is deliberate and five beats apart.
- **ONE terracotta per beat**, and it always marks the machine-written share.
  Citations are always muted ink.
- **Typing appears in exactly two beats**: B00 and B12 (B04 is the one sanctioned
  ask micro-beat of the ask→result pair).
- **Every figure carries its citation in frame.** No component in this reel can
  render a number without one.

## What a human could optionally upgrade

Nothing is required, but the slot contract still applies: dropping
`media/B0X.mp4` (or `pantry/B0X-916.mp4` for the vertical) and re-running
`./art run` swaps any beat by filename without touching the edit.
