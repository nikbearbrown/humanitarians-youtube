# scenes.py — intentionally empty.
#
# This reel is PURE REMOTION: every beat carries shot.remotion.pattern and is
# rendered by runtime/scripts/remotion_scenes.py. There are no Manim beats, so
# there are no Scene classes here. run.sh requires the file to exist before it
# will compile a reel, so it stays as this comment.
