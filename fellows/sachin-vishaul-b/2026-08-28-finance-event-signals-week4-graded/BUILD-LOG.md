# BUILD-LOG — "Claude, Graded." (Week 4)

Session date: 2026-08-31 · Toolkit: `brutalist.art` (cli-explainer skill) ·
Cost: $0.00 · Register: Teardown, claude-liam channel.

## What was built

12-beat `cli-explainer` reel over the real Week 4 build of
`finance-event-signals` — the series finale. Same Claude-skin/GitHub-skin
split and shared toolkit fixes as Weeks 1-3 (see Week 1's BUILD-LOG.md for
the `npx` PATH shim, QC burn-in mask, and em-dash audio-encoding bug).

## Content-specific note

B05-B07 is not a bug-fix revision like Weeks 1-3 — it's the deliberate
adversarial test the project's attestation format requires (feed the
grader two exotic ticker types on purpose to see if it crashes or
fabricates instead of admitting it can't price them). Both real results
(3/3 correct on the gradeable set; 0 crashes on the adversarial test) are
quoted directly from the source project's `RUN_LOG.md`.

## Series-closing note

This is the last of the four work videos. The recipe's final state
(`RUNNABLE-LIVE`, not `VERIFIED`) is stated in B09 exactly as the source
project's own `GOVERNANCE.md` states it — no upgrade for a better closing
line.

## Known gaps in this submission

- **9:16 cut not built.**
- **PROOF-REVIEW: pending.**
