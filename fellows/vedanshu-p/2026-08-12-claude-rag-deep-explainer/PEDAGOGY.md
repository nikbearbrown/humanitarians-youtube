# PEDAGOGY — Two Kinds Of Memory. (deep-explainer, personal author channel)

Deep-explainer (5–10 min documentary cut) of *RAG Foundations* Chapter 1 —
the multi-act treatment of the same source as the `ai-explainer` and
`cli-explainer` sibling reels. Personal author channel: persona and sign-off
are the book's author, Vedanshu Daxesh Patel (`@VedanshuDaxeshPatel`), voice
Kokoro `am_onyx` ("Onyx"), free. IN-FOR-BEAR LAW does not apply.

**Tier note.** `deep-explainer` is also listed ADVANCED (Bear-only) in this
toolkit's root `CLAUDE.md`, rule 8 — same tier as `cli-explainer`, built
earlier in this session on the same basis: the requester's direct, explicit
instruction, given after that gate was surfaced once already this session.

## Act structure (6 acts, 31 body beats + 4 bookends = 34 total)

See `beat_sheet.json` metadata `acts` + full beat list; summarized in
`BUILD-LOG.md`. Full authoring detail (lane mix, vox runs, Gate F, Gate D2)
lives in `BUILD-LOG.md` per this skill's own output contract — this file
exists specifically so the shared `generate_audio_kokoro.py` GATE P check
(which looks for `PEDAGOGY.md`) has something to check, consistent with the
sibling reels.

## VERDICT: PASS

Human requested this build directly (deep-explainer, same chapter, same
voice/signature as the sibling reels). Proceeding to Gate F (factcheck,
closed — see FACTCHECK.md), audio lock, and the Gate D1 previz.
