# CHECKS-REPORT — agentic-design-patterns-part-1

Written BEFORE the first slate compiled, per ai-explainer PROOF GATE.

## Beat classification

**12 SHOW / 2 justified-HOLD / 0 PUNT-flagged**

| Beat | Class | Artifact named in `shot.show` |
|---|---|---|
| B00 | SHOW | composer ask types, running indicator, three output lines land |
| B01 | SHOW | hesitant writer types `prompting`, corrects to `architecture` |
| B02 | SHOW | six pattern cards land in two rows (what runs / how it thinks) |
| B03 | SHOW | 5-node chain builds; Validate gate lights terracotta |
| B04 | SHOW | router + three specialist branches fan out |
| B05 | SHOW | split → 3 workers → merge converges |
| B06 | SHOW | dashed feedback arc curves Critic → Generator |
| B07 | SHOW | permission gate lights with read-only chip |
| B08 | SHOW | goal branches to milestones + constraint check |
| B09 | SHOW | four patterns compose across one ticket |
| B10 | HOLD | three conditions land, then one lights — a judgment beat; the three cards ARE the claim |
| B11 | SHOW | artifact page, six verdict lines land in order |
| B12 | SHOW | suggested prompt types itself into the composer |
| B13 | HOLD | title restate + rule + handle — the outro is a held card by OUTRO LAW |

No beat is a bare CARD. Every beat that makes a structural claim names its
on-screen artifact.

## Teaching arc

| Item | Status | Where |
|---|---|---|
| FRAMEWORK before examples | ✓ | B02 presents all six as a set before B03–B08 explain any |
| WORKED EXAMPLE | ✓ | B09 — one support ticket carried through four composed patterns |
| FALSIFIABILITY | ✓ | B10 — the three conditions under which a single call beats a pipeline |
| SCAFFOLDED TASK | ✓ | B12 — map YOUR shipped feature to the six; ranked recommendation demanded |
| BOOKENDS | ✓ | B00 cold open · B01 BLUF · B11 verdict · B12 handoff · B13 outro |
| NO-SOURCE-NO-VERDICT | ✓ | every trade-off claim traces to the source doc; see FACTCHECK.md |

## Legibility contract

- Every SHOW beat names its artifact in `shot.show`.
- `AgenticPatternDiagram` positions from the shared `SAFE` constant; nothing is
  pixel-nudged. Cards size to content with padding; labels are 1–3 words.
- One terracotta moment per beat (`accentId`) — ACCENT LAW.
- Negative space: the grid stretches to fill SAFE (FILL-THE-CANVAS), so the
  55% ink-coverage floor Gate V enforces is met by construction, not by luck.

## Deviations logged

1. **ASK→RESULT LAW applied at the bookends, not per illustration.** A strict
   reading would put a composer micro-beat before each of the nine illustrated
   beats. That produces 9 extra UI beats and directly violates ILLUSTRATE LAW's
   anti-wallpaper rule ("two consecutive beats sharing the same visual scheme is
   the smell"). Read as governing *demoed generated artifacts* — the concept
   illustrations here are the teaching, not a demo of Claude's output. The cold
   open is a true ask→result pair. Flagged for review.

2. **`ClaudeTitleOutro` not used.** It hardcodes `@NikBearBrown` with no prop
   (OUTRO-LOCK.md). Built `HaiTitleOutro` instead — same OUTRO LAW shape (title
   restate · terracotta period · handle beneath), correct channel, no mascot.

3. **`lead_silence_s: 0.8` on B01** is recorded per EXECUTIVE-SUMMARY LAW but is
   an authoring annotation the current pipeline does not consume. B01's audio
   window is verified ≥9s instead, which is what the law is protecting.
