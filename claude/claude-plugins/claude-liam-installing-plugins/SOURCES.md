# SOURCES — Claude, Configured (Ch.2)

## Source document
- `books/anthropics/books/claude-cowork-plugins/chapters/ch02-installing-customizing.txt`
  (from `claude-cowork-plugins.md`, Chapter 2 — *Claude Cowork Plugins*, rev. Spring 2026, "John Collins")

## Corrections applied (DOUBLE-CHECK LAW; see book-level FACTCHECK.md)
| In source | On screen / in narration | Why |
|---|---|---|
| "Cloud" / "Cloud's" (transcription error, throughout) | "Claude" | Speech-to-text artifact. |
| "Co-Work" / "co-works" | "Cowork" | The product is one word. |
| "ThropX official collection" | "an official collection" | Transcription artifact for "Anthropic's"; roster name dropped so no count leaks in. |
| "in-current" (housekeeping note) | "keeping current" / updates | Transcription artifact for "keeping current." |
| "spend 20 minutes customizing it" | "spend a few minutes" | Illustrative duration kept generic — avoids over-specifying. |
| "org-wide plugin management is coming, but not yet fully available" | (cut) | Dated roadmap claim; would age the episode. B21 keeps only the evergreen "plugins are local." |
| Plugin count / full roster | "an official collection plus community additions" | The count has grown past 11 (FACTCHECK §2); any number dates the reel. |
| Prices, "Mac and Windows" | (never shown) | Datable per FACTCHECK §4 — no price beat, no platform list; "in your sessions" / "on your machine." |
| "TikTok scripts" (book example) | "viral video scripts" | De-branded a named third-party platform in the contrast example; kept as a fleeting illustration only. |

## Claims carried (verified true / evergreen)
- Install is a guided, no-code flow — browse a library, click install, it downloads/activates/is ready. ✓ (FACTCHECK #8)
- Custom/shared plugins arrive as files you upload through the plugins interface. ✓
- After install, capabilities appear in sessions: skills auto-activate, slash commands appear in the command menu, connectors are ready to configure. ✓
- Customization is a plain-language guided conversation (not technical configuration); the plugin calibrates from your answers. ✓
- Some plugins are self-contained (productivity); others are far more useful connected to your existing tools (sales→CRM, data→database/warehouse) via a standard authorization/login flow. ✓ (FACTCHECK #7 — tool categories kept generic, named products avoided)
- Disable preserves customization/config; plugins improve over time via updates; most people keep two or three active; plugins and their config are stored locally on your machine. ✓
- A quick post-install test (list slash commands / ask a domain task) shows a live plugin's answer is more structured and domain-aware. ✓

## Named-example handling (FACTCHECK §4)
- CRM, database, warehouse, LinkedIn posts, monthly newsletter, competitive scan, content calendar appear ONLY as fleeting illustrative examples inside the marketing-customization walkthrough (the book's own example) — never as a beat's subject, never presented as a fixed fact list.

## Seeds / determinism
- Manim + Remotion scenes are pure functions of the audio clock; log any RNG seed here at render time.
- No AI-generated real people in this reel; all six VOX stills are Tier 1 illustrative (no rights escalation).

## Credits
- Narration: Liam (in for Bear), Kokoro `am_onyx`. Channel: @NikBearBrown.
