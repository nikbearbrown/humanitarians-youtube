# SOURCES — Claude, Built (Ch.12)

## Source document
- `books/anthropics/books/claude-cowork-plugins/chapters/ch12-building-your-own.txt`
  (from `claude-cowork-plugins.md`, Chapter 12 — *Claude Cowork Plugins*, rev. Spring 2026, "John Collins")

## Corrections applied (DOUBLE-CHECK LAW; see book-level FACTCHECK.md)

### Datable / stale — compressed to generic mechanism
| In source | On screen / in narration | Why |
|---|---|---|
| "Beyond the 11 official plugins" | "the official catalog" (no count) | Roster has grown past 11 (FACTCHECK §2); a count dates the reel. |
| "the plugin directory at Claude.com/plugins" | "the wider directory" | URL/platform string dropped — dates the reel; the mechanism (a public directory) is what matters. |
| Named third-party tools | "your pricing spreadsheet," "a specific folder" | Kept generic — no brand names as a beat's subject (FACTCHECK §4). |
| "Plug-in create" (a specific product string) | "a guided builder — itself a plugin you install" | Named as mechanism, not a versioned product name, to stay evergreen. |

### Transcription artifacts (speech-to-text; not the author's) — cleaned before narration
| In source | Corrected |
|---|---|
| "clawed" / "Claude" mix (and the book-wide "Cloud" → "Claude") | **Claude** |
| "co-work" / "Cowork" | **Cowork** (one word) |
| "solar-proners" | **solopreneurs** |
| "Enboard a new development client" | **Onboard** |
| "Slinging the anatomy" | **Knowing the anatomy** |
| "the plug-and-s is" (book-wide) | **the plugin is** |

*Note:* Chapter 12's raw text did not contain the literal "Cloud"-for-"Claude" swap in-line, but the book-wide artifact is logged here per the DOUBLE-CHECK mandate; any residual instance is normalized to **Claude** in narration.

## Claims carried (from source; conceptual spine verified in FACTCHECK.md)
- A custom plugin encodes a personal workflow (steps, standards, decision points) so Claude runs it consistently. ✓
- Building is guided and low/no-code — a conversation with a builder, not editing config files. ✓
- A plugin bundles up to four component types: skills, commands, connectors, subagents. ✓ (subagents added per the book's four-component model / this reel's one-idea, though Ch.12 prose emphasizes the first three.)
- Plugins are file-based (plain text + config), hence easy to share, modify, and read. ✓
- Build candidates: repeated steps, consistency-critical work, low-thought admin, "I wish I could hand this off." ✓
- Plugins can be shared (team / peers / public directory) and should evolve as living documents. ✓

## Seeds / determinism
- Manim + Remotion scenes are pure functions of the audio clock; log any RNG seed here at render time.
- No AI-generated real people in this reel; all six vox stills (B03, B04, B08, B13, B18, B23) are Tier 1 illustrative (no rights escalation, no identifiable people, no logos).

## Credits
- Narration: Liam (in for Bear), Kokoro `am_onyx`. Channel: @NikBearBrown.
